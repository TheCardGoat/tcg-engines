# One Piece Simulator

One Piece engine, card data, types, utilities, and import tooling for TCG
Online.

## Development

- Check everything is ready:

```bash
vp run ready
```

- Run the tests:

```bash
vp run test -r
```

- Build the monorepo:

```bash
vp run build -r
```
