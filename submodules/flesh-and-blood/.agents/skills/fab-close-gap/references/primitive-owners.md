# Primitive owners (closer playbook)

One line per learned stamp site, primitive owner, or regression ban. Merge
duplicates. Classify reads this before proposing a new discriminant. Obsolete
catalog-stringification guards were removed; regression bans live in
[test quality policy](../../fab-test-generation/references/quality.md) and typed
engine inventory tests.

- [status] wizard Ice fusion riders stamp `dealt-damage-to-hero` / `targets-a-hero` / `targets-a-frozen-ally` on the deal-damage packet (hero vs living object; Ally is a subtype). Not combat `attack-target`. Polar Cap Frostbite uses `controller: "target-controller"` after that stamp. Icevein "they discard a card" is `discard` `random: true`. Encase freeze of a hero is `selector: "opponent"` — `packages/engine/src/rules/proposals/asset-effects.ts` (2026-08-20)
- [status] 6-power this-way riders count printed `{p}` ≥ 6 on the discard/banish cohort. Additional-cost GY banish stamps `banished-this-way` onto the play layer. Pound of Flesh is `for-each` each-hero, not a global boolean — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/engine/src/procedures/play-card/payment.ts` (2026-08-20)
- [status] do not add a `FAB_STATUS_MARKERS` slug for “N or more X this turn” — author `compare-amount` per `references/decision-tree.md`; grandfathered slugs are snapshotted in `packages/engine/src/rules/has-status-coverage.test.ts`
- [binding] fusion reveals stamp `fused-with-earth/ice/lightning-card` (and `fused-this-way`) on the fuse/play layer, same shape as pitched-this-way talent stamps — ELE091 / ELE092 — `packages/engine/src/procedures/play-card/fuse.ts` (2026-08-21)
- [binding] pitch-to-play talent stamps include Chi; Ice/Lightning bond catalog reads `pitched-this-way-ice/lightning-card` — PEN211 / PEN212 / MST079 — `packages/engine/src/procedures/play-card/finalize.ts` (2026-08-21)
- [latch] “if the next attack is fused, instead +N / gain dominate” is a second `appliesTo.next` latch with `hasStatus: fused`, not a sibling static on the grant source — ELE122 / ELE154 — `packages/cards/src/cards/ELE/actions/ELE122-weave-earth-red.test.ts` (2026-08-21)
- [amount] “if this has 6 or more {p}” is `compare-amount` `subject-property` current power (subject falls back to source) — SUP149 — `packages/engine/src/rules/evaluation/amounts/subject-property.ts` (2026-08-21)
- [history] “if you've intimidated this turn” is `intimidated-an-opponent-this-turn`, already stamped on intimidate banishes — HVY018 — `packages/engine/src/game/turn-history.ts` (2026-08-21)
- [filter] printed “aura of suspense” is `control-object` `typeBox.subtypes:Aura` + `hasKeyword:suspense`, never `name: "Aura of Suspense"` — SUP178 / SUP169 — `packages/cards/src/cards/SUP/actions/SUP178-full-of-bravado-red.test.ts` (2026-08-22)
- [amount] “if this has 10 or more {p}” is `compare-amount` subject-property current power, never `has-10-or-more-power`. Do not print `keywords:[overpower]` for that rider. Attack-flow play options include `xValue` — EVO140 — `packages/cards/src/cards/EVO/actions/EVO140-moonshot-yellow.test.ts` (2026-08-22)
- [history] “if this was banished from your hand this turn” is object-history `hand` → `banished` this turn on the subject (CR 3.0.9c), grandfathered as `has-status` delegate — DTD109 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` (2026-08-21)
- [status] unhandled `has-status` stays fail-loud; unknown filter markers still fail closed so defend matching does not abort
- [instead] printed same-layer “instead” is `self-replacement` (CR 6.4.7). Future-event “would … instead” is `replacement` / `prevention`. Never fold `conditional.instead` — [repository migration ledger](../../../../../../docs/fab-instead-migration-ledger.md)
- [instead] same-layer amount swap keeps the preceding declared target / `appliesTo` on the modification; do not reanchor `selector:self` at proposal time — `packages/engine/src/rules/proposals/effects/sequence.ts`
- [amount] `turned-face-down-this-way` / `put-into-graveyard-this-way` are counts stamped by turn-face-down and GY moves (destroy/discard/move-card), not status slugs. Chart mill+Gold is one sequence — `packages/engine/src/rules/evaluation/amounts/count.ts` (2026-08-21)
- [binding] optional “turn a card in their GY face-down; if it's yellow, create Gold” needs turn-face-down `outputBinding: "it"` (proposal already stamps it). Decline leaves `it` empty so the rider no-ops — SEA138 / SEA139 / SEA140 — `packages/engine/src/rules/proposals/counter-status-effects.ts` + `packages/cards/src/cards/SEA/actions/SEA138-pilfer-the-wreck-red.test.ts` (2026-08-22)
- [binding] granted “attack with it an additional time” targets `self` of the granted ability (Take a Stab). Create-then-counters stamps create-token `it`. Deal-damage must not clobber a prior sequence `it` (Gateway reveal then banish it). “Choose X equipment” is `any-number` on equipment seats — FNG027 / MST153 / MON164 / PEN268 / SEA087 (2026-08-20)
- [target] on-stack freeze `player: "target-controller"` can silent-no-op at `play()` (card stays in hand) — UPR119 Icebind; do not treat that as success
- [fix] module `keywords:` must equal the printed keyword line — unprinted `goAgain` (runtime slug `go-again`)/`overpower`/`dominate`/`opt` makes conditionals unconditional
- [assert] evaluated color is `expectFabCard.toHaveColor("Red"|"Yellow"|"Blue"|null)` from the rules view (CR 2.1); Blanch hit-strip uses `null` — `packages/engine/src/testing/fluent-assert.ts` (2026-08-21)
- [clash] “when you win a clash revealing this” is clash-win observing the winner’s reveal (CR 8.5.45a). clash-outcome follow-up must set actorId=winner and affected=[winner’s card] only — copying both deck-tops plus the clash initiator as actor cheers the loser. Arena “when you/they win a clash” stays observes:none — `packages/engine/src/rules/reducers/mechanics.ts`
- [play] illegal announce (no legal target, unpayable additional cost) reverses CR 5.1.5 / 5.1.8a and fails the fluent — `expectFabUnplayable` — `packages/engine/src/testing/test-engine.ts` (2026-08-21)
- [target] freeze without printed “target” is at-resolution after deal-damage stamps `target-controller`; `playerMatches` reads that binding. On-stack freeze at announce is illegal encoding — UPR119 Icebind (2026-08-21)
- [target] named public zones (arsenal/graveyard/banished) are legal on-stack seats (CR 1.8.5a). Fixture arsenal defaults face-down; seat `{ faceDown: false }` for face-up arrows — Point the Tip DYN168 (2026-08-21)
- [combat] “if this is defended by an action, +N{p}” is while-static on the chain, not a play-time ability.condition — MST191 Rowdy Locals (2026-08-21)
- [combat] “defended by a Reviled/Revered/Guardian/Brute card” is the same while-static + `defendedByTypeBoxToken` (types or supertypes). Dishonor’s three named cards are `control-object` on combat-chain/permanent, never a compound status slug — SUP033 / SUP089 / SUP141 / SUP184 / OUT051 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/SUP/actions/SUP033-fight-fair-red.test.ts` (2026-08-22)
- [combat] printed “when 1 or more Brute/Guardian cards defend this” observes `defender` with class `supertypes` (class lives in collectSupertypes). Printed “when this defends a Reviled/Revered hero's attack” keeps `defended-attack` plus a hero-identity type-box; matcher applies that filter to the **attacking** hero only for Reviled/Revered. “Shadow attack” / “Brute attack” match the attack card. Do not put `amount:gte 1` on a single-defender event — SUP142 / SUP185 / SUP062 / SUP124 / DTD094 / SUP210 — `packages/engine/src/rules/trigger-matcher.ts` + `packages/cards/src/cards/SUP/actions/SUP142-familiar-stench-red.test.ts` (2026-08-23)
- [target] `playerMatches` and `playerIdsForWho` share CR player seats (`defending-hero`, `target-controller` from the stamped string). Do not throw on defending-hero — `packages/engine/src/rules/evaluation/helpers.ts` (2026-08-21)
- [target] on-stack “allies they control” reads `player: "target-controller"` from the first declared hero/player; play/activate must pass `declaredTargets` so the ally scan is not 0 candidates — UPR179 / UPR180 / UPR181 — `packages/engine/src/runtime-targets.ts` + `packages/cards/src/cards/UPR/actions/UPR179-singe-red.test.ts` (2026-08-21)
- [target] exact-count at-resolution with `pool.length <= N` is determined (CR 1.8.6c); “up to” still opens 0..N. Choose-card from a looked cohort of size > 1 is a player pick, not bind-all — Intimate Inducement MST017 / Headbutt MPG016 (2026-08-21)
- [harness] `.target(card)` names the object (determined no-op; default attack-proxy, `{ identity: "source" }` for the original). Empty `.target()` is choose-none on min=0. Do not probe `getState().decision` or `advanceToDecision(..., "entity-target")` — `packages/engine/src/testing/target-identity.ts` (2026-08-21)
- [harness] `play`/`playAttack` `{ from: banished|arsenal|deck|graveyard }` resolves the instance across seats (`findCardInPlayOrigin`); begin-play already accepts a cross-owner origin. Prefer the actor's own zone when both seats hold a copy. — MST002 / HER113 — `packages/engine/src/testing/test-engine.ts` + `packages/cards/src/cards/heroes/nuu.test.ts` (2026-08-30)
- [target] `playerIdsForWho` / `playerMatches` use seated `facts.playerIds`, not `heroRefs` keys — a rules-test seat without a hero object is still an opponent (2026-08-21)
- [target] choose-card pick-1-from-cohort is only for a `binding` with count 1. Count-N zone scans use the generic 1.8.6c path. Printed “Target N” is `declared: "on-stack"` (DTD106). Sequence prefix simulation covers determined move-card/discard, not only draw (PEN154) (2026-08-21)
- [target] printed “target ash” on Silken Form is on-stack (CR 1.8.5 / 5.1.4), declared before the destroy-self cost is paid — DRO007 (2026-08-21)
- [picker] a second `--next-cluster` without `--exclude` returns the same densest cluster (duplicate-pick stall). Closed/skipped ids must be passed as `--exclude`; do not `await_user` — `packages/cards/scripts/card-coverage/select-next-cluster.ts` (2026-08-20)
- [harness] reorder-deck partition decisions declare ONE group (`effect.position`, bottom-first; entries top-most first); drain partition answers must come from `fabListedPartitionGroups` (single bottom-first group flips the listed entries; Opt top/bottom keeps `fabOptPartitionGroups`) — the old Opt-shaped `{top,bottom}` answer fails "Use only legal partition groups" and re-pends — `packages/engine/src/testing/intent.ts` + AZL014/022/025 (2026-08-27)
- [latch] printed “next attack action card you play this turn” is `this-attack` + `appliesTo.next` (CR 6.2.4). Dummy at-resolution stack/combat-chain scans are leftover parser shape; remaining 1 is consumed on the first matching play — `packages/engine/src/rules/continuous/reconciler.ts`
- [latch] printed “next AAC with 2 or less base {p} you play this combat chain gains go again” is `appliesTo.next` numeric `basis: "base"` plus `events:["play","attack"]`. Drop empty `and` dummy filters. `this-combat-chain` duration dies at first-link resolve; use `this-turn` so the next link still sees the latch — EVR044 / EVR045 / EVR046 — `packages/cards/src/cards/EVR/actions/EVR044-ride-the-tailwind-red.test.ts` (2026-08-22)
- [hit] printed “the next time an AAC hits a hero this turn, it deals N damage to them” is resolution `delayed-trigger` windowed `this-turn` `matching:first` on `hit`, never resolution `deal-damage` `appliesTo.next` (that evaluates with no attack-target). Deal-damage target is `defending-hero`. Weapon hits do not match Action type-box — ELE198 / ELE199 / ELE200 — `packages/cards/src/cards/ELE/actions/ELE198-electrify-red.test.ts` (2026-08-22)
- [latch] “attack action cards you control gain +1{p} and +1{d} this turn” is unbounded `appliesTo.next` `count: all` (power on play/attack; defense events include defend). Do not snapshot hand/chain star-count and also latch — that double-applies. Unbounded latches lock the current attack as a plain object ref so later plays survive that link. Prove +1{d} with a 3{d} AAC vs 4{p} (Snatch 2{d}+1 still takes 1 from 4{p}) — ARC160 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/ARC/instants/ARC160-art-of-war-yellow.test.ts` (2026-08-21)
- [latch] printed “cards you own with crush get dominate this turn” is the same unbounded `appliesTo.next` `count:all` `grantKeyword(dominate)` filtered `hasLabel:crush`, never an at-resolution hand/arsenal snapshot (that does not ride onto the attack-proxy). Seat Valda as P2 so start-phase sees the Surge tokens. — EVR019 / HER135 — `packages/cards/src/cards/heroes/valda-brightaxe.test.ts` (2026-08-30)
- [latch] “defense reaction cards cost opponents an additional {r}” is `appliesTo.next` DR `count: all` with `target: opponent` (observesOpponent). A hand snapshot plus the latch double-taxes — SUP000 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/SUP/resources/SUP000-authority-of-ataya-blue.test.ts` (2026-08-21)
- [latch] “next aura you play as an instant; if you do, it enters with a +1{p} counter” is play-card `appliesTo.next` plus enter-arena replacement `appliesTo.next`, not optional-then replacement of `self` (the leaving card) — MST155 / MST156 / ENG022 — `packages/cards/src/cards/MST/actions/MST155-vengeful-apparition-red.test.ts` (2026-08-21)
- [opt] printed “Opt N, then X” keeps `keywords:[opt(N)]` and one resolution `opt` leaf so Opt precedes the follow-up. Do not synthesize a trailing keyword Opt when the AST already has one (Sharpen pattern; Blood Tribute) — `packages/engine/src/rules/reducers/zone-moves/helpers.ts`
- [binding] printed “If you do, it gets …” after look-then-arsenal targets binding `it` (the arrow), not `self` of the Action — SEA103 / SEA104 / SEA105 — `packages/cards/src/cards/SEA/actions/SEA103-hook-blue.test.ts` (2026-08-21)
- [effect] printed “choose a color” is a public `effect-resolution` Red/Yellow/Blue chooser (name-card pattern). Do not auto-bind red. Grant-property color `"chosen"` locks from `chosen-color` at proposal time — PEN040 / PEN041 / PEN042 — `packages/engine/src/rules/proposals/effects/choose-color.ts` + `packages/cards/src/cards/PEN/actions/PEN040-become-the-cup-red.test.ts` (2026-08-21)
- [binding] printed “choose a card on the combat chain. This gets the chosen card's name” is choose-card then grant-property name `"chosen"`. choose-card stamps `named-card` from the chosen object's names. Combat-chain unique self is a legal pick (do not rewrite to hand) — PEN037 / PEN038 / PEN039 — `packages/engine/src/rules/proposals/effects/choose-card.ts` + `packages/cards/src/cards/PEN/actions/PEN037-become-the-bottle-red.test.ts` (2026-08-22)
- [combat] granted “If this is defended by an attack action card, this gets Go again” is a while-static on the next attack (Barraging Beatdown / Rowdy Locals), not a resolution ability. `defended-by-attack-action` is the existing combat-fact handler, sibling of `defended-by-action` — HVY127 / HVY128 / HVY129 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/HVY/actions/HVY127-engaged-swiftblade-red.test.ts` (2026-08-21)
- [combat] granted “If this weapon is defended by an attack action card, it gains +1{p} until end of turn” is a Cintari-style defend triggered-static, not a resolution ability — MON113 / MON114 / MON115 — `packages/cards/src/cards/MON/actions/MON113-plow-through-red.test.ts` (2026-08-21)
- [amount] printed “a card has been put into your banished zone this turn” is `zone-count` `zone:banished` `per:turn` (history moves), not a this-turn status slug — MON254 / MON255 / MON256 — `packages/engine/src/rules/evaluation/conditions/zone-count.ts` + `packages/cards/src/cards/MON/actions/MON254-tremor-of-i-arathael-red.test.ts` (2026-08-21)
- [prevention] printed “the first time you prevent damage this way, create a token” is `prevention.additionalModification` create-token (CR 6.4.10h), not a this-way status slug. “If you prevent damage to another hero this way” wraps that create-token in `conditional` `target-exists` `{ selector:"hero", who:"another-hero" }` evaluated at damage time; choose-hero is `shielded: any-hero` (Oasis); `times:1` is “the next time”. Instant `{r}, discard this` prevent-then-Lightning-Flow is the same additionalModification, never resolution `has-status prevented-damage-this-way` — OMN184 / OMN169 / OMN170 / OMN171 / TCC058 / TCC062 / TCC075 — `packages/engine/src/kernel/replacements/apply.ts` + `packages/cards/src/cards/OMN/instants/OMN184-calmveil-of-volthaven-red.test.ts` (2026-08-22)
- [amount] printed “less {h} and fewer equipment and tokens than them” is `life-comparison` plus `compare-amount` of equipped-objects + token zone-count, not a less-than status slug — HVY213 / HVY214 / HVY215 — `packages/cards/src/cards/HVY/actions/HVY213-down-but-not-out-red.test.ts` (2026-08-21)
- [binding] “if an item or equipment was banished from boosting this” is `binding-matches` on play-event `boostBanished` (Item subtype / Equipment type), stamped on the boost banish — EVO192–197 — `packages/engine/src/procedures/play-card/boost.ts` (2026-08-21)
- [amount] “if you've wagered this chain link” is `compare-amount` `wagers-this-chain-link`, not a status slug. Weapon attacks are `types:Weapon` as well as `subtypes:Attack` — HVY112 — `packages/engine/src/rules/evaluation/amounts/count.ts` (2026-08-21)
- [wager] an attack-reaction wager without an explicit attacker uses the active chain attack (`this-attack`), not the reaction layer source — MPW042 / MPW043 / MPW044 — `packages/engine/src/rules/proposals/mechanic-effects.ts` + `packages/cards/src/cards/MPW/attack-reactions/MPW042-donkey-blue.test.ts` (2026-08-21)
- [play] “costs {r} less if it targets a Shadow card” recomputes play-static reduction after targets with bindings `it`; empty `it` is optimistic at quote — MON084 — `packages/engine/src/procedures/play-card/declarations.ts` (2026-08-21)
- [history] “if you've pitched a blue card this turn” is `pitch-zone-has` `color:["blue"]` (pitch zone is this-turn pitches), not a this-turn status slug — MST014–016 / MST020–022 / MST063–065 — `packages/engine/src/rules/evaluation/evaluate-condition.ts` + `packages/cards/src/cards/MST/attack-reactions/MST014-hiss-red.test.ts` (2026-08-21)
- [history] “if you've created a card this turn” is `performed-this-turn` event `create-card` on the turn ledger's `create` events — PEN270 — `packages/engine/src/game/turn-history.ts` (2026-08-21)
- [replacement] “if a go fish effect would trigger, instead it triggers twice” is a canonical trigger-count ×2 replacement expanded at trigger collection — SUP268 — `packages/engine/src/kernel/trigger-collection.ts` (2026-08-21)
- [cost] soul-banish X additional cost is payable at ≥1 soul card; the chooser min is 1 (X can't be 0) — MON033 — `packages/engine/src/procedures/play-card/effect-costs.ts` (2026-08-21)
- [cost] required “banish N from soul” is exact-N: reverse when soul < N, auto-bind when pool = N (CR 1.8.6c), chooser otherwise. Do not stamp 0 / `upTo` for a printed exact count — MON062 — `packages/engine/src/procedures/play-card/declarations.ts` + `packages/cards/src/cards/MON/actions/MON062-celestial-cataclysm-yellow.test.ts` (2026-08-22)
- [cost] optional “you may banish a card with blood debt from your hand; if you do, go again” is `isOptionalBanishHandPlayCost` (filtered hand banish, not random, not GY). Unpayable auto-declines; pay then choose (or auto-bind 1). Do not print unconditional `goAgain` — MON156 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/MON/actions/MON156-shadow-of-ursur-blue.test.ts` (2026-08-22)
- [cost] optional destroy any-number additional cost is first-class (`isStarDestroyPlayCost`). Public path is `playInstance(..., "explicit")` then `accept()`/`decline()` then `.target`, not `modeIds:["pay"]`. `destroyed-this-way` stamps the paid cohort — EVR158 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/EVR/actions/EVR158-cash-out-blue.test.ts` (2026-08-21)
- [cost] “remove three +1{p} counters from among auras rather than pay {r}” is play-static `alternative-cost` `remove-counters` numeric +1 power ×3. Quote already treats a payable alternative as covering printed resources. Fixture `powerCounterTotal` is +1{p}; `powerCounters` is a named “power” counter and will not pay — MST131 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/MST/actions/MST131-10-000-year-reunion-red.test.ts` (2026-08-22)
- [cost] optional “pay up to N{r}” is payable when available ≥ 1 even if available < N; skip the may only at 0 resources. Destroy that many uses `differentNames: true`, not `hasStatus: different-names`. Count is `resources-paid-this-way`; +{p} is `destroyed-this-way` — SUP215 — `packages/engine/src/rules/layer-resolution/decisions/optional.ts` + `packages/cards/src/cards/SUP/actions/SUP215-cutting-retort-red.test.ts` (2026-08-21)
- [amount] “Destroy target landmark with cost X. Create X Gold” binds X from the destroyed landmark’s cost (`outputBinding: "it"` then `count: { type: "reference", binding: "it", property: "cost" }`). It is not a play-cost X / `play({ xValue })`; filter `cost eq X` before X exists is chicken-and-egg — PEN327 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/PEN/actions/PEN327-ransack-and-raze-blue.test.ts` (2026-08-21)
- [amount] “X is arcane damage you've dealt this turn” is `count` `damage-dealt` `damageType:arcane` `per:turn`. Vaporize is optional destroy cost≤X (exclude token metatype) and/or up-to-X aura tokens, `player:any`. Exact-X token destroy aborted the sequence when no tokens existed. Meld is right then left with priority between faces — ROS011 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/ROS/instants/ROS011-vaporize-shock-yellow.test.ts` (2026-08-21)
- [amount] “for each 4 Copper, 2 Silver, and/or 1 Gold destroyed this way, repeat” is `repeat.times` `sum` of `destroyed-this-way` counts with `divisor` 4/2/1, not a metal-token-units status. Play-cost destroy must stamp the cohort as object snapshots (not only a count) so filtered counts can LKI. `resolveRepeatTimes` evaluates FabAmount via `resolveLayerAmount`. Optional cost is `any-number`, names Copper/Silver/Gold — EVR159 — `packages/engine/src/rules/proposals/resolve-repeat-times.ts` + `packages/engine/src/procedures/play-card/payment.ts` + `packages/cards/src/cards/EVR/actions/EVR159-knick-knack-bric-a-brac-red.test.ts` (2026-08-21)
- [filter] “destroy an aura token they control” is `typeBox` `metatypes:Token` + `subtypes:Aura`, never `name: "Aura"`. Next-AAC latch stays `this-attack` + `appliesTo.next` — OMN092 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/OMN/actions/OMN092-leech-renown-red.test.ts` (2026-08-21)
- [token] printed “create A or B token” is `choose-and-create-token` with two slugs, never a compound `a-or-b` token id. Printed “create A and B” is two `create-token` leaves (Fang Strike and Slither into hand), never `fang-strike-and-slither`. Magrar is `zen-state` then `inertia`, never `zen-state-and-an-inertia`. “create a Confidence and 3 Might” is `confidence` then `might` `count:3`. Hit “create A and B” is a sequence of two leaves. “Repeat this process X more times” is `repeat` `times:{type:x}` after the first create. `{x}` plus N is `evaluateBoundVariable` `bound * count + plus` — ROS004 / OMN189 / MST010 / JDG038 / SUP008 / SUP070 / SUP075 / SUP078 — `packages/engine/src/rules/proposals/effects/choose-and-create-token.ts` + `packages/cards/src/cards/ROS/actions/ROS004-germinate-blue.test.ts` (2026-08-22)
- [target] fused “deal arcane to that hero” on a non-attack action is `deal-damage` `selector: "any-hero"` (declared play target), never combat `attack-target`. Unresolved attack-target reverses the whole layer including the Frostbite create — UPR109 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/UPR/actions/UPR109-ice-eternal-blue.test.ts` (2026-08-21)
- [amount] “if you control 3 or more Gold, create that many Might” is `compare-amount` of `cards-in-zone` Gold ≥ 3 then create-token `count` of the same zone-count, never `event-amount` and never `hasStatus: control-3-or-more-gold`. Sequence evaluates the count after the minted Gold — MST225 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/MST/actions/MST225-visit-goldmane-estate-blue.test.ts` (2026-08-21)
- [trigger] fused “whenever the defending hero adds a defending card to this chain link” is a fused resolution `delayed-trigger` on `defend` observing `defender` with `windowed` `this-chain-link` `matching: every`. Never `hasStatus: added-to-this-chain-link`. Defend source selector is `defender`, not `attack`. Frostbite controller is `defending-hero` — ELE004 — `packages/engine/src/cards.ts` + `packages/cards/src/cards/ELE/actions/ELE004-endless-winter-red.test.ts` (2026-08-21)
- [target] “that hero gains 1 action point” after negate is `gain-action-points` `target: "target-controller"`, never combat `attack-target`. Negate stamps `it` and `target-controller` (owner). Asset gains pass layer bindings into `playersForFabPlayer`. Rewind returns the negated card to hand via `move-card` `it` — UPR169 — `packages/engine/src/rules/proposals/effects/negate.ts` + `packages/engine/src/rules/proposals/asset-effects.ts` + `packages/cards/src/cards/UPR/instants/UPR169-rewind-blue.test.ts` (2026-08-21)
- [binding] “If you do, it gains +1{p}” after putting an arrow into arsenal targets binding `it` (the arrow), never `self` of the bow. Split from grant-permanent-zone (DR-from-hand on chain arrows) — EVR087 — `packages/cards/src/cards/EVR/weapons/EVR087-dreadbore.test.ts` (2026-08-21)
- [grant] “Arrows you control have ‘DR can’t be played from hand this chain link’” is a Widowmaker-style resolution `restrict`/`defend` granted onto controller arrows in `combat-chain`+`stack`, never `zones:["permanent"]`. The reconciler projects granted window rules onto the active attack (`grantedWindowRuleAbilities`). Play-quote `playedFromZones` is the origin zone (hand), not a completed stack move — EVR087 — `packages/engine/src/rules/continuous/reconciler.ts` + `packages/engine/src/rules/legality/play.ts` + `packages/cards/src/cards/EVR/weapons/EVR087-dreadbore.test.ts` (2026-08-21)
- [replacement] GY “if Frostbites would be created, instead you may banish this” is `isOptionalDestroySelfCreateReplacement` (destroy or banish self). `functionalZones:["graveyard"]` + duration `permanent`, never `while-in-arena`. Apply from GY emits banish, not destroy — PEN251 — `packages/engine/src/kernel/replacements/admission.ts` + `packages/engine/src/kernel/replacements/apply.ts` + `packages/cards/src/cards/PEN/attack-reactions/PEN251-smoldering-steel-red.test.ts` (2026-08-21)
- [trigger] “hits a Runeblade or Wizard hero” is Crix-style `target.kind:hero` plus `observes.filter.or` of class **supertypes** (Wizard/Runeblade are collectSupertypes, not types). `filterAppliesToHeroTarget` must recurse `or`/`and` or an or-only filter matches the attack instead. Aura identity is `subtypes:["Aura"]`. Hit events stamp `defendingPlayerId` for heroTargetObject — SUP257 — `packages/engine/src/rules/trigger-matcher.ts` + `packages/engine/src/procedures/combat/combat-damage.ts` + `packages/cards/src/cards/SUP/actions/SUP257-mage-hunter-arrow-red.test.ts` (2026-08-21)
- [trigger] “the next Ice or Elemental attack that hits a hero” must keep Attack in the delayed-hit filter (`subtypes:Attack` + Ice/Elemental supertypes) so `filterAppliesToHeroTarget` does not steal it onto the defending hero — ELE163 / ELE164 / ELE165 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` (2026-08-21)
- [latch] “your next attack this turn gets +1{p}” (`events:["attack"]`) must latch an announced Attack-subtype play so on-stack resolution sees current {p}. `subject-property` falls back to the condition source. Flex Speed 6+{p} go again stays `compare-amount` — SUP146 / SUP147 / SUP148 / TCC105 — `packages/engine/src/rules/continuous/reconciler.ts` + `packages/engine/src/rules/evaluation/amounts/subject-property.ts` + `packages/cards/src/cards/SUP/actions/SUP146-flex-speed-red.test.ts` (2026-08-21)
- [grant] “when you attack with this, deal 1 arcane” granted onto the next AAC is `observes: { kind: "source", selector: "attack" }`, never `filter.name: "This"`. Latch+same-window observe already fire; prove with `playAttack(..., { stopAt: "on-attack" })` then `.target(hero)` (any-hero candidates are player ids) — CHN014 / CHN016 / MON185 / OMN086 — `packages/engine/src/kernel/continuous-reconcile.ts` + `packages/cards/src/cards/CHN/actions/CHN014-seeds-of-agony-red.test.ts` (2026-08-21)
- [history] “if you've created a Fealty token this turn” is `performed-this-turn` `create-fealty-token`, already stamped at create — HNT153 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/HNT/actions/HNT153-march-of-loyalty-red.test.ts` (2026-08-21)
- [history] “if you've activated a cannon this turn” is `performed-this-turn` `activate-cannon`, stamped on activate when the source type box has Cannon (type or subtype). Never `hasStatus: activated-a-cannon-this-turn` — OMN241 / PEN166 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/OMN/actions/OMN241-red-lure-harpoon-blue.test.ts` (2026-08-22)
- [amount] “+X{d} while defending, where X is twice the number of clashes you've won this turn” is `count` `clashes-won-this-turn` doubled. Stamp on `clash-win`. Block cards on the combat chain must keep continuous statics functional (not only Equipment / Defense Reaction / Attack) — HVY060 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/engine/src/rules/continuous/reconciler.ts` + `packages/cards/src/cards/HVY/blocks/HVY060-boast-blue.test.ts` (2026-08-22)
- [target] “Banish all defending attack action cards on the combat chain” scans `player: "any"` on `combat-chain` with `defending: true`. Default controller seat misses the opponent’s defending AAC — MST008 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/MST/attack-reactions/MST008-gorgon-s-gaze-yellow.test.ts` (2026-08-22)
- [combat] “if this has {p} greater than its base” is `object-numeric-comparison` current gt base, not `power-greater-than-base` — HVY066 / ELE209 — `packages/cards/src/cards/HVY/actions/HVY066-over-the-top-yellow.test.ts` (2026-08-21)
- [amount] “if you have boosted this combat chain” is `compare-amount` `count` `boosts-this-combat-chain` ≥ 1, never `hasStatus: boosted-this-combat-chain`. Boost stamps the chain count even when the attack is still being announced (combat may not be open yet) — EVR076 / EVR077 / EVR078 — `packages/engine/src/rules/reducers/mechanics.ts` + `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/EVR/actions/EVR076-payload-red.test.ts` (2026-08-21)
- [amount] “if you've hit with a dagger this combat chain” is `compare-amount` `count` `attacks-hit-this-combat-chain` filtered to `subtypes:["Dagger"]`. Hit outcomes stamp `sourceWasDagger` and a combatNumber (weapon chains may open without one). Never `hasStatus: hit-with-dagger-this-combat-chain` — PEN147 / PEN148 / PEN149 — `packages/engine/src/rules/reducers/combat.ts` + `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/PEN/actions/PEN147-knife-through-red.test.ts` (2026-08-21)
- [binding] play-pitch Earth/Ice/Lightning/Chi “this way” stamps from pitch events **and** `pitchedInstanceIds` (definition types included). Activate-only snapshot was missing Earth on Log Fall. Split from applies-to-next-color-filter — TER011 / TER015 — `packages/engine/src/procedures/play-card/finalize.ts` + `packages/cards/src/cards/TER/actions/TER011-log-fall-red.test.ts` (2026-08-21)
- [filter] token name filters use the printed name (`"Toughness"`), never `"Toughness Token"`. Glory Plate / Escalate already did; Heroic Grit grant count is the same `name` + Token metatype — SUP056 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/SUP/actions/SUP056-heroic-grit-yellow.test.ts` (2026-08-21)
- [latch] “your next blue attack this turn gets …” is `appliesTo.next` `color:["blue"]`. A Red Snatch still draws on hit (printed), which is not Tenet latching; prove extra-draw absence with 1 card in hand, and a non-drawing red AAC for 0 — ENG018 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/ENG/actions/ENG018-first-tenet-of-chi-moon-blue.test.ts` (2026-08-21)
- [target] printed “target Illusionist attack you control” is `object` `declared:on-stack` `zones:["combat-chain"]` with Illusionist+Attack, never `this-attack` (no announce target). Split from negate-phantasm stacked trigger — UPR154 — `packages/cards/src/cards/UPR/instants/UPR154-semblance-blue.test.ts` (2026-08-21)
- [negate] “Negate all phantasm triggered effects of target attack” is `negate` `triggeredKeyword: "phantasm"`: remove stacked `keyword:phantasm` triggered layers sourced from that object (do not GY the attack). Then remove-property phantasm and restrict gain-keyword on `it`. Intervening-if 8.3.13a is not negate — UPR154 — `packages/engine/src/rules/proposals/effects/negate.ts` + `packages/cards/src/cards/UPR/instants/UPR154-semblance-blue.test.ts` (2026-08-22)
- [cost] printed cost X omits catalog `cost` (numeric-only) and binds `{ type: "x" }` in the resolution amount. Do not invent a numeric cost. `usesPrintedXResourceCost` is that pair — HVY250 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/HVY/instants/HVY250-reel-in-blue.test.ts` (2026-08-21)
- [filter] “search for a card named Minnowism” is filter `name: "Minnowism"` (i18n printed name), never `"Card Named Minnowism"`. Catalog `base.names` is the slug; matches-filter also derives the printed identity. Play-cost `then` search/shuffle is a triggered layer at payment, not a skipped compile — MON266 / MON267 / MON268 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/engine/src/procedures/play-card/payment.ts` + `packages/cards/src/cards/MON/actions/MON266-belittle-red.test.ts` (2026-08-21)
- [filter] printed “a Mechanologist item / red Mechanologist AAC / off-hand with Proclamation in its name” is `typeBox` (Item subtype + Mechanologist supertype; AAC color+Action+Attack) or `nameContains`, never a prose `name:` phrase — EVO072 / EVO081–083 / JDG030 — `packages/cards/src/cards/EVO/actions/EVO072-quantum-processor-yellow.test.ts` (2026-08-22)
- [filter] printed “Play only if you control a Blasmophet” is `control-object` `nameContains: "Blasmophet"` (Herald/Sigil pattern). Exact `name: "Blasmophet"` never matches “Blasmophet, the …” tokens or “Blasmophet, Levia Consumed”. — IAR009 / IAR010 — `packages/cards/src/cards/actions/consuming-lash.test.ts` (2026-08-30)
- [filter] printed “whenever you attack with a weapon this turn” is resolution `delayed-trigger` `typeBox.types:Weapon` plus bind `it`, never `name: "A Weapon"` and never a GY static. Pair the end-phase strip in the same sequence so the second delayed-trigger still registers. `remove-all-counters` numeric +1{p} emits `numeric-counter-removed`; weapon seats are `zones:["weapon"]` — EVR056 — `packages/engine/src/rules/proposals/effects/remove-all-counters.ts` + `packages/cards/src/cards/EVR/actions/EVR056-oath-of-steel-red.test.ts` (2026-08-22)
- [filter] “action with cost less than this card's {p}” is numeric cost `lt` `subject-property` power (source fallback), never a fake `hasKeyword` — OUT185 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/OUT/actions/OUT185-give-and-take-red.test.ts` (2026-08-21)
- [search] “search your deck … then shuffle and put it on top” is one `search` `to:deck top`. The proposal already shuffles the remainder first (CR 8.5.19/8.5.20). A sibling `shuffle` after search-to-top buries the card — SUP258 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/SUP/actions/SUP258-take-the-bait-red.test.ts` (2026-08-21)
- [search] “search for a Chi … put it on top” moves the Chi, never `self` of the resolving Action. Extra `move-card` `self` puts Look Within on the deck instead of the graveyard — PEN273 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/PEN/actions/PEN273-look-within-blue.test.ts` (2026-08-21)
- [trigger] 1v1 “start of each other hero's turn, choose 1” is start-phase actor `opponent` plus modal choose-1. Mirror gifts are delayed-triggers until the controller's next start-phase, not party each-other-hero or a this-way status — LSS006 — `packages/cards/src/cards/LSS/actions/LSS006-good-deeds-don-t-go-unnoticed-yellow.test.ts` (2026-08-21)
- [filter] “Destroy up to 3 Might tokens” is at-resolution `name: "Might"` + Token metatype (printed name, never `"Might Token"`). Token registry `base.names` is that printed name; single-word slugs title-case in matches-filter — SUP025 — `packages/engine/src/testing/token-registry.ts` + `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/SUP/instants/SUP025-renounce-violence-blue.test.ts` (2026-08-21)
- [search] 1v1 “each hero may search … Potion or Brew … If two or more heroes put a card into the arena this way, go again” is for-each each-hero optional search (nameContains) plus `compare-amount` `put-into-arena-this-way` ≥ 2. Do not print unconditional `goAgain`. Search-to-permanent stamps that this-way cohort — LSS023 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/LSS/actions/LSS023-drinking-buddy-red.test.ts` (2026-08-21)
- [keyword] printed “If you control 2 or more Draconic chain links, this gets go again” is resolution `compare-amount` `chain-links` Draconic ≥ 2 then grant-property. Never `keywords:[goAgain]` — FAI016 / UPR064 / FAI026 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/FAI/actions/FAI016-cinderskin-devotion-red.test.ts` (2026-08-21)
- [duration] “your hero gets -2{i} during your next end phase” is `during-own-next-end-phase` (`player-end-phase-window`, atoms apply only in that end phase). Draw-to-i is the end-phase proof. Do not remodel to until-end-of-own-next-turn — ROS217 — `packages/engine/src/rules/continuous/runtime.ts` + `packages/cards/src/cards/ROS/actions/ROS217-ten-foot-tall-and-bulletproof-red.test.ts` (2026-08-21)
- [become] “Name a living legend hero. Become that hero … except your base {h} doesn't change” is `name-card` then `become` `named-hero` as a stage-1 copy onto the controller hero (`frozenSource` from the named catalog definition, `except: base-life`, `observeAsBecome`). Do not emit observation-only become — PEN121 — `packages/engine/src/rules/evaluation/atoms/become.ts` + `packages/cards/src/cards/PEN/instants/PEN121-embody-greatness-yellow.test.ts` (2026-08-21)
- [amount] “for each hero dealt damage this way” is `count` `heroes-dealt-damage-this-way`, stamped from committed deal-damage packets (post-prevention) on the same sequence. Split resolution abilities lose this-way. Never a status slug — HVY252 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/HVY/actions/HVY252-aether-arc-blue.test.ts` (2026-08-21)
- [token] 1v1 “Create a token under each other hero's control” is `controller: "each-other-hero"` (the sole opponent). `each` mints under both seats — PEN150 / TCC064 / TCC066 / TCC067 — `packages/engine/src/rules/evaluation/helpers.ts` + `packages/cards/src/cards/PEN/actions/PEN150-song-of-larinkmorth-white-blue.test.ts` (2026-08-21)
- [token] printed “under target hero's control” is `create-token.target` `selector: "any-hero"` plus `controller: "target-controller"`. `controller: "any"` mints under every seat. Polar Cap binds the same seat from a prior deal-damage target. Fused enter-arena Frostbite is the same pair; pause the enter trigger then `.target(hero)` — UPR144 / UPR145 / UPR146 / ELE025 / ELE026 / ELE027 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/UPR/actions/UPR144-arctic-incarceration-red.test.ts` (2026-08-22)
- [token] “create a Frostbite in an exposed head, chest, arms, or legs zone” is `amongExposed` on create-token (Jarl), not arena mint. `toHaveTokenCount` only counts arena; assert the equipment seat. Iyslander on an opponent's turn also mints Frostbite for Ice plays — PEN226 — `packages/cards/src/cards/PEN/instants/PEN226-frost-spike-blue.test.ts` (2026-08-22)
- [create] “Put a Cracked Bauble from outside the game on top of target hero's deck” is `create-card` with `target: any-hero` and `to: { zone: "deck", position: "top" }`. Lookup matches catalog slugs minus pitch color. Split from repeat-star-banish — LGS099 — `packages/engine/src/rules/proposals/effects/create-card.ts` + `packages/cards/src/cards/LGS/actions/LGS099-shitty-xmas-present-yellow.test.ts` (2026-08-22)
- [repeat] “you may banish 1{p} from each hero's GY; if you do, +1{p}, then repeat” is `repeat` `until: "declined"` wrapping optional + `for-each` `each-hero` banish `iteration-subject`. Never `set-status repeat-this-process`. findDecision must stop further iterations after a declined optional — HNT221 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/HNT/actions/HNT221-rotten-remains-blue.test.ts` (2026-08-22)
- [repeat] “Destroy an aura with cost X or less, then you may destroy a Seismic Surge you control. If you do, repeat this process” is `repeat` `until: "declined"` wrapping a **sequence** (required destroy then optional Surge). Body-first sequence is not optional-first like Rotten Remains. Never `set-status repeat-this-process`. findDecision must not scan iteration N+1 until N accepted; proposeRepeat must unroll the next iteration only after that accept and keep per-iteration event groups — HNT248 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/engine/src/rules/proposals/effects/repeat.ts` + `packages/cards/src/cards/HNT/actions/HNT248-roiling-fissure-blue.test.ts` (2026-08-22)
- [choose] “Each hero secretly chooses a number between 1 and 6 … the hero that chose the highest loses that much {h} and searches cost ≤ that number” is `choose-number` min/max/chooser/secret, then for-each `compare-amount` vs `highest-chosen-number` unique. Never `set-status secretly-chose-a-number`. Search chooser is `effect.player` (iteration-subject). Tie at the maximum pays no unique-highest hero (singular “the hero”). Opponent chooses first — HNT255 — `packages/engine/src/rules/proposals/effects/choose-number.ts` + `packages/cards/src/cards/HNT/actions/HNT255-spur-locked-blue.test.ts` (2026-08-22)
- [target] “up to X target heroes and/or allies, where X is the number of Sigil auras you control” is on-stack `upTo` plus `count` `cards-in-zone` Aura nameContains Sigil. Runtime `evaluateAmount` must resolve FabAmount (not only literals / all / any-number). X=0 auto-binds no targets so the Action still resolves — PEN111 — `packages/engine/src/runtime.ts` + `packages/engine/src/procedures/play-card/declarations.ts` + `packages/cards/src/cards/PEN/actions/PEN111-glyph-destruction-nodes-yellow.test.ts` (2026-08-22)
- [activate] mixed `alternative` activation costs are a player declaration (CR 5.1.3c) via `alternativeCostIndex`. A **unique payable** arm is determined when the index is omitted. Two payable arms still require the index. “If this attack has wagered” is `compare-amount` `times-it-has-wagered`, never `has-status attack-has-wagered` — MPW007 — `packages/engine/src/procedures/activate-ability/costs/parse.ts` + `packages/cards/src/cards/MPW/weapons/MPW007-golden-grail.test.ts` (2026-08-22)
- [delay] “banish face-down from their hand; at the beginning of their next end phase, return it” is banish `outputBinding: "it"` plus `delayed-trigger` end-phase `actor: opponent` `until-triggered` move-card `it` to hand. Delayed sources stay functional after the arrow leaves the chain. “Their next end phase” is the defending hero’s turn, not the attacker’s `endTurn` — AAZ016 — `packages/engine/src/rules/snapshots.ts` + `packages/cards/src/cards/AAZ/actions/AAZ016-stone-rain-red.test.ts` (2026-08-22)
- [enter] “enters with two +1{p} counters; if Chi was pitched to play this, instead four” is one enter-arena `add-counter` replacement whose count is a conditional FabAmount (pitched-this-way-chi-card). Never `conditional.instead` wrapping two replacements — the static collector only admits a replacement leaf. Apply resolves the count from the enter-arena event’s play-layer bindings — MST031 — `packages/engine/src/kernel/replacements/apply.ts` + `packages/cards/src/cards/MST/instants/MST031-manifestation-of-miragai-blue.test.ts` (2026-08-22)
- [control] “Each opponent chooses an item or landmark they control. Gain control of the chosen permanents” is for-each `each-other-hero` choose-card `player`/`chooser: iteration-subject` then gain-control of `it`. `player: "self"` is the ability controller. Missing `it` after an empty choose is `[]`, not unresolved. Do not keep a trailing gain-control outside the loop — JDG003 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/JDG/equipments/JDG003-proclamation-of-requisition.test.ts` (2026-08-22)
- [amount] printed “Phoenix Flames you control” for Rise Up rupture +X is `count` `cards-in-zone` `combat-chain` named Phoenix Flame, never arena `permanent` (chain-link attacks are not arena permanents). X is twice that count — FAI007 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/FAI/actions/FAI007-rise-up-red.test.ts` (2026-08-22)
- [replacement] printed “instead you reveal the top of their deck and they reveal yours” is replacement-only `swap-clash-reveals` (not a sequence of reveal leaves). `proposeFreshClashEvents` swaps deck sources when that replacement is registered; if you win, the optional prize (discard) is a deferred clash-prize. Never `has-status won-clash` on the Instant layer — SUP087 — `packages/engine/src/kernel/replacements/admission.ts` + `packages/engine/src/rules/proposals/mechanic-effects.ts` + `packages/cards/src/cards/SUP/actions/SUP087-the-old-switcheroo-blue.test.ts` (2026-08-22)
- [search] printed “search your deck for any number of cards named X” is `search` `count: { type: "any-number" }` (0..pool), never `all` (auto-take every match). `resolveSearchCount` must resolve quantifiers from the matching zone pool. Unprinted `goAgain` on Mugenshi: RELEASE makes combo go-again unconditional — WTR083 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/WTR/actions/WTR083-mugenshi-release-yellow.test.ts` (2026-08-22)
- [activate] omitted `abilityId` on a source with both a non-attack Action and an Attack activation selects the unique non-attack; `activateAttack` selects the unique Attack. Non-random `choose-and-create-token` is an effect-resolution for `chooser` (opponent first on Smash Hit) — TCC050 — `packages/engine/src/procedures/activate-ability/stages/quote.ts` + `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/TCC/weapons/TCC050-jinglewood-smash-hit.test.ts` (2026-08-22)
- [activate] an object with no functional activated ability is quote `handled` denied `no_activated_ability`, never `handled:false` unmigrated. Zombies without Vox, Dragons without Storm of Sandikai, and equipment with no activation use `expectActivationRejected`. — IAR064 — `packages/engine/src/procedures/activate-ability/stages/quote.ts` + `packages/cards/src/cards/actions/restless-magister.test.ts` (2026-08-30)
- [latch] “your next sword attack gets +N and ‘when this attacks, wager…’” must latch on a weapon **attack activation** (not only the later combat `attack` event) so the granted trigger is on the sword before the attack observation. Sword filters need Weapon+Sword type-box. Wager prize `player: "winner"` reads the winner binding; empty `winner` seats skip the GY equip. Equip dest is the equipment seat, never `zone: "graveyard"` — MPW033 — `packages/engine/src/rules/continuous/reconciler.ts` + `packages/engine/src/rules/evaluation/helpers.ts` + `packages/cards/src/cards/MPW/actions/MPW033-rake-back-blue.test.ts` (2026-08-22)
- [harness] Opt after `activate()` is CR 8.5.22 partition. `untilIdle({ ordering: "listed" })` keeps looked cards on top; `activate({ optBottom: N })` matches `play({ optBottom })`. Default drain still throws so playAttack explicit-drive stays loud — ARC037 — `packages/engine/src/testing/intent.ts` + `packages/cards/src/cards/ARC/actions/ARC037-optekal-monocle-blue.test.ts` (2026-08-22)
- [choose] printed “choose 2 … opponent chooses 1” is two `choose-card` steps (`them` then `that-card` with `chooser: "opponent"`) plus `exclude: "that-card"` on the remainder. Exact-N with pool < N persists empty and stages `[]` (do not re-scan the zone). `objectTargets` treats explicit empty persist as no-op, not unresolved. After defend the attacker has priority — `passBoth` then controller `.target` then opponent `.target` — MPG126 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/MPG/actions/MPG126-call-for-backup-red.test.ts` (2026-08-22)
- [look] printed “look at their hand and choose a blue card” is look-all then `choose-card` from binding `revealed-this-way` with a color filter, never `look.choiceFilter` (ignored; bound the whole hand as `it`). Add-defending / if-you-do draw the chosen card only — MST009 — `packages/cards/src/cards/MST/attack-reactions/MST009-siren-s-call-red.test.ts` (2026-08-22)
- [look] printed “choose a card without base {d}” is `lacksProperty: "defense"`, never `hasStatus: no-base-defense`. Pick-1-from-cohort pending targets must `and` `inObjectBinding` with the choose-card filter. Discard the bound `it`. Hit-look chooser: `closeCombat({ entityTargets: "pause" })` then `.targetRequired` — MST192 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/MST/actions/MST192-the-weakest-link-red.test.ts` (2026-08-22)
- [opt] printed “Opt N. Banish the top…” is `keywords:[opt(N)]` plus a leading resolution `opt` leaf before banish (Blood Tribute). Keyword-only Opt does not restack before the same-layer banish. Resolution Opt after `play()` is answered by `untilIdle({ optBottom: N })` / `{ ordering: "listed" }` — IAR212 / IAR213 / IAR214 — `packages/cards/src/cards/IAR/actions/IAR212-pull-from-beyond-red.test.ts` (2026-08-22)
- [prevention] printed “you may banish this to prevent N” on equipped Legs/Head/Chest/Arms is `optionalCost` `banish-self`, never `banish` `from: "arena"` (arena scan misses equipment seats). Shroud/Cloak/Grasp already used banish-self — DTD168 — `packages/cards/src/cards/DTD/equipments/DTD168-dance-of-darkness.test.ts` (2026-08-22)
- [replacement] printed “if an opponent would draw 1 or more during your action phase, instead destroy this and they draw that many minus 1” is `replaces.player: opponent` draw + destroy-self. Draws are one-card-per-event; minus 1 cancels that event. Only during the controller’s action phase (`state.phase` + `activePlayerId`). Own draws do not fire — EVR192 — `packages/engine/src/kernel/replacements/admission.ts` + `packages/cards/src/cards/EVR/actions/EVR192-talisman-of-tithes-blue.test.ts` (2026-08-22)
- [restrict] printed “Choose an equipment. It can't defend this turn” is restrict-defend with on-stack equipment `subject`, never `hasStatus: "it"`. Latched subjects are the defender, not the attack (`object.ref.instanceId`). Enter/leave “target equipment can't activate” is the same on-stack subject. Split from status/it — EVO073 — `packages/engine/src/rules/legality/defense.ts` + `packages/cards/src/cards/EVO/actions/EVO073-stasis-cell-blue.test.ts` (2026-08-22)
- [restrict] printed “the only actions … are weapon and attack actions” is restrict-play of `typeBox.types: Action` `excludeSubtypes: Attack`, never restrict of the allow-list (that inverts). Existing `quoteFabPlay` already denies filter-matching cards. Game-level (empty subjects) applies to every hero — JDG025 — `packages/engine/src/rules/legality/play.ts` + `packages/cards/src/cards/JDG/equipments/JDG025-proclamation-of-combat.test.ts` (2026-08-22)
- [restrict] printed “effects can't increase arcane damage that target action on the stack would deal” is restrict `modify-damage` `damageType: arcane` with on-stack Action `subject`. Damage-amount-boost replacements (Metacarpus / Rousing Aether plus N) no-op when that source is latched — SEA261 — `packages/engine/src/rules/unpreventable-damage.ts` + `packages/cards/src/cards/SEA/instants/SEA261-arcane-compliance-blue.test.ts` (2026-08-22)
- [amount] printed “If 3 or more cards are turned face-down this way” is `compare-amount` of existing `count` `turned-face-down-this-way` ≥ 3, never `has-status 3-or-more-turned-face-down-this-way`. Hit the Gas AP already uses that count; turn-face-down already stamps it. Seat Maxx for specialization; fixture Hyper Drivers `{ faceDown: false }` — SUP255 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/SUP/actions/SUP255-hit-the-gas-blue.test.ts` (2026-08-22)
- [combat] printed “If this is attacking a hero” is `has-status attacking-a-hero` as a **resolution conditional** (combat open, subject is the active attack, target is the defending hero). Do not put it on `ability.condition` — play quote evaluates before combat and either throws or skips the rider. Handler is sibling of `attacking-a-marked-hero` / `attacks-a-light-hero` — SUP021 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/SUP/actions/SUP021-a-good-clean-fight-red.test.ts` (2026-08-22)
- [combat] printed “While this is attacking, defending, or on the stack” is `has-status attacking-defending-or-on-the-stack` on a while-static: true only for **this object** as active attack, a defender, or `zone:stack`. False in hand or the while would throw at every play. Sibling `attacking-or-on-the-stack` omits defending. Do not treat seating as combat — OMN088 / OMN089 / OMN090 / OMN214 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/OMN/actions/OMN088-arcanic-cunning-red.test.ts` (2026-08-22)
- [combat] printed “AACs get -1{p} while attacking or defending” is filter `hasStatus: attacking-or-defending` (live active attack or defender). Chain-close to GY is leave-arena (CR 3.0.5); attack “when this leaves the arena” is that event, not a post-leave status re-check — PEN249 / PEN133–135 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/engine/src/rules/reducers/zone-moves/helpers.ts` + `packages/cards/src/cards/PEN/actions/PEN249-haboob-red.test.ts` (2026-08-22)
- [history] printed “Activate this only if a card has entered a graveyard this turn” is `zone-count` `zone:graveyard` `player:any` `per:turn` ≥ 1, never `has-status card-entered-graveyard-this-turn`. Fixture GY is not this-turn. Instant activate needs reaction-window priority (`advanceCombatTo("reaction")`) — EVR181 — `packages/engine/src/rules/evaluation/conditions/zone-count.ts` + `packages/cards/src/cards/EVR/actions/EVR181-amulet-of-oblation-blue.test.ts` (2026-08-22)
- [binding] printed “If you complete a contract this way” is `has-status completed-contract-this-way` as a **same-layer** conditional after the banish, never a second resolution ability (play quote would skip or throw). complete-contract stamps the resolving layer binding `"true"`. Seat Arakni for specialization; look/reorder is `choosePartition` after `passBoth` — HVY246 — `packages/engine/src/rules/reducers/mechanics.ts` + `packages/cards/src/cards/HVY/attack-reactions/HVY246-coercive-tendency-blue.test.ts` (2026-08-22)
- [amount] printed “If you control N or more Phoenix Flames” is `compare-amount` of `count` `cards-in-zone` `combat-chain` named Phoenix Flame, never `has-status control-N-or-more-phoenix-flames` (or repeated-subject). Same zone as Rise Up; unprinted `keywords:[goAgain]` made the 1+ rider unconditional — UPR048 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/UPR/actions/UPR048-phoenix-form-red.test.ts` (2026-08-22)
- [history] printed “If you've controlled an Agility token this turn” is `control-object` `filter.name: Agility` `per:turn`, never `has-status controlled-agility-this-turn`. Live arena Agility satisfies currentlyControls; Vigor/Might already have this-turn stamps — SUP219 — `packages/engine/src/rules/evaluation/conditions/control-object.ts` + `packages/cards/src/cards/SUP/actions/SUP219-beat-the-same-drum-blue.test.ts` (2026-08-22)
- [amount] printed “the next time you deal that much or more {p} damage to a hero” is delayed `event-and-state` `deal-damage` `damageType:physical` plus `compare-amount` `trigger-event-damage` ≥ `reference` `chosen-number`, never `has-status dealt-power-damage-gte-chosen-number`. Event-and-state eval must merge delayed `triggerBindings` and stamp `trigger-event-damage` from the deal-damage packet. Choose-number 1–20 is an effect-resolution `choose("N")` after `passBoth` — HVY136 — `packages/engine/src/rules/condition-evaluator.ts` + `packages/cards/src/cards/HVY/actions/HVY136-talk-a-big-game-blue.test.ts` (2026-08-22)
- [history] printed “Play only if an Illusionist AAC you control has been destroyed by phantasm this turn” is play-static `performed-this-turn` `phantasm-destroy-illusionist-attack-action`. Stamp on destroy when the event source has phantasm and the destroyed object is Illusionist+Action+Attack. Never `has-status destroyed-by-phantasm-this-turn-…`. Unplayable regex is `/play condition is not satisfied/` — UPR153 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/UPR/actions/UPR153-frightmare-red.test.ts` (2026-08-22)
- [history] printed “if an item you control has been destroyed this turn” is `performed-this-turn` `destroy-item` (stamp `history.turn.destroyedItem` on destroy of Item type/subtype). Never `has-status item-you-control-destroyed-this-turn`. “Action cards get -1{d} while defending this” is a this-chain-link while-static gated by that history **and** `attacking` so galvanize-defense does not tax itself — EVO105–107 / EVO141 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/EVO/actions/EVO105-ratchet-up-red.test.ts` (2026-08-22)
- [restrict] printed “AACs can't gain {p} from their own effects, or the effects of attack reaction cards” is restrict `gain-power` with `subject` AAC type-box and `source: "self-or-attack-reaction-effects"`, never `hasStatus: from-own-or-attack-reaction-effects`. Numeric power-add skips when the generating source is the AAC itself or an Attack Reaction. Blanket crush (Chokeslam) is the same action with no source gate — CRU182 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/CRU/instants/CRU182-snag-blue.test.ts` (2026-08-22)
- [history] printed “Activate only if you haven't played a card or activated an ability this turn” is `not` `performed-this-turn` `play-or-activate`. Stamp that flag on play and activate. Never `has-status havent-played-or-activated-this-turn`. Quote runs before this activation stamps — EVR179 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/EVR/actions/EVR179-amulet-of-ignition-yellow.test.ts` (2026-08-22)
- [amount] printed “while your hero is the target of a source that would deal damage ≥ your {h}” is `compare-amount` of `count` `pending-damage-to-hero` vs `hero-property` current life, never `has-status hero-targeted-by-lethal-or-more-damage`. Pending combat damage is remaining {p} on the open attack targeting that hero. `hero-property` life reads `playerLife`, not printed base {h} — EVR180 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/EVR/actions/EVR180-amulet-of-intervention-blue.test.ts` (2026-08-22)
- [activate] printed “Activate only while this is in your banished zone” is `functionalZones:["banished"]` on the activated ability, never `has-status in-your-banished-zone`. Quote denies when the source catalog zone is outside `functionalZones`; legal-commands enumerates banished the same way as arsenal. Blood Debt is a separate end-phase static. Seat Levia — MON192 — `packages/engine/src/procedures/activate-ability/stages/quote.ts` + `packages/engine/src/rules/legal-commands.ts` + `packages/cards/src/cards/MON/defense-reactions/MON192-guardian-of-the-shadowrealm-red.test.ts` (2026-08-22)
- [activate] printed GY Instant “activate only while it's in your graveyard … less {h} than each hero … cheered this turn” is `functionalZones:["graveyard"]` plus `life-comparison` vs other heroes (self is not in the compared set) plus `been-cheered-this-turn`. Helm of the Adored on the defending action cheers; attacker `pass` then `passBoth` then Instant. Snatch defending is 2{d}+3 — APS015 — `packages/engine/src/rules/evaluation/conditions/life-comparison.ts` + `packages/cards/src/cards/APS/blocks/APS015-never-give-up-yellow.test.ts` (2026-08-22)
- [activate] GY Instant “Destroy 2 Silver you control: Equip this” is `functionalZones:["graveyard"]` plus destroy-cost `count` ≥ 1 (not only 1). Mixed Action + AR omitted `abilityId` selects the unique Action; two Action abilities still need `abilityId`. Off-Hand GY equip defaults to weapon1 — PEN136 / ARC010 / ARC018 / CRU105 — `packages/engine/src/procedures/activate-ability/costs/parse.ts` + `packages/engine/src/procedures/activate-ability/stages/quote.ts` + `packages/cards/src/cards/PEN/equipments/PEN136-graven-gaslight.test.ts` (2026-08-22)
- [amount] printed “if you have less cards in arsenal than an opposing hero” is `compare-amount` of `count` `cards-in-zone` arsenal controller vs opponent `lt`, never `has-status less-cards-in-arsenal-than-opposing-hero`. 1v1 opposing hero is `player: "opponent"` — EVR188 — `packages/cards/src/cards/EVR/actions/EVR188-talisman-of-balance-blue.test.ts` (2026-08-22)
- [binding] printed “If an attack action and a non-attack action were pitched to play this, it has …” is a play `event-and-state` grant using the existing `pitched-attack-and-non-attack-action-to-play-this` layer stamp (DYN172), never a while-static (those evaluate without play bindings). Stamp already lives in play finalize / activate payment — DYN173 — `packages/cards/src/cards/DYN/weapons/DYN172-annals-of-sutcliffe.test.ts` (2026-08-22)
- [amount] printed “If 1/2/3+ Chi were pitched to play this” is `compare-amount` of binding `pitched-this-way-chi-count` (play-event stamp from pitch events / pitchedInstanceIds), never `has-status pitched-N-chi-to-play-this`. While-statics miss play bindings — use play `event-and-state` then set-base {p}. Count unique Chi instance ids — MST076 — `packages/engine/src/procedures/play-card/finalize.ts` + `packages/cards/src/cards/MST/actions/MST076-cosmic-awakening-blue.test.ts` (2026-08-22)
- [history] printed “unless you've pitched, played, or defended with a blue card this turn” is `or` of `pitch-zone-has` blue, `played-this` blue, and `zone-count` combat-chain this-turn blue. AND of the same three is transcend. Never `has-status pitched-played-or/and-defended-…`. “The first attack that targets you” is `appliesTo.next` `hasStatus: targeting-you` `events:["attack"]` `ordinal:1`, never `targets-you` — MST000 — `packages/cards/src/cards/MST/instants/MST000-mistcloak-gully.test.ts` (2026-08-22)
- [amount] printed “if they have played 2 or more cards with the same name this turn” is `compare-amount` of `count` `cards-played-this-turn` `groupBy: "name"` for `player: "opponent"` ≥ 2, never `has-status played-2-or-more-same-name-this-turn`. 1v1 “they” on an Instant item is the opponent — EVR177 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/EVR/actions/EVR177-amulet-of-echoes-blue.test.ts` (2026-08-22)
- [history] printed “If this was played from arsenal” scans **any** object-history move from arsenal, not only the last (stack → chain clobbers origin, same as played-from-banished). While-static +1{p} stays `has-status played-from-arsenal`. Seat `{ faceDown: false }` for face-up arsenal plays — ELE134 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/ELE/actions/ELE134-burgeoning-red.test.ts` (2026-08-22)
- [combat] printed “cards with {p} greater than the attack they are defending get +1{d}” is a while-this-defending grant onto combat-chain `defending: true` whose filter hasStatus `power-greater-than-attack-defending` compares the candidate's current {p} to the active attack {p}. Sibling of `defense-greater-than-attack-power`. Unhandled filter markers fail closed — MPG127 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/MPG/actions/MPG127-captain-of-the-guard-blue.test.ts` (2026-08-22)
- [binding] printed “If an attack action / non-attack action is revealed this way” is `binding-matches` on reveal `outputBinding: "it"` (reveal also stamps `revealed-this-way`). AAC is `types: Action` `subtypes: Attack`; non-attack is `types: Action` `excludeSubtypes: Attack`. Never `has-status revealed-this-way-*-action-card` — EVR156 — `packages/cards/src/cards/EVR/actions/EVR156-bingo-red.test.ts` (2026-08-22)
- [restrict] printed “target hero can't play DRs with the same name as a card in their graveyard” is restrict-play `subject: any-hero` plus filter `hasStatus: same-name-as-a-card-in-their-graveyard` (sibling of defending-hero-banished). Play quote applies a hero-latched restrict to that hero's plays. Name match reads the DR owner's GY. Play `{ target: Bravo }` — OUT187 — `packages/engine/src/rules/legality/play.ts` + `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/OUT/actions/OUT187-burdens-of-the-past-blue.test.ts` (2026-08-22)
- [amount] printed “When this attacks, if it scrapped a card” is `compare-amount` of `count` `cards-scrapped-by-this` ≥ 1, never `has-status scrapped-a-card`. Scrap banish stamps `scrappedCard` / `cards-scrapped-by-this: 1` onto the play layer **and** a `declarationFacts` `{ kind: "scrap" }` on the played incarnation so later attack triggers still see it. Hyper Driver is not a crank item; Backup Protocol is. Seat Teklovossen. `{ scrap: true, scrapCard }` — EVO132 / EVO133 / EVO134 / EVO135 — `packages/engine/src/procedures/play-card/scrap.ts` + `packages/engine/src/rules/reducers/zone-moves/move.ts` + `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/EVO/actions/EVO132-scrap-harvester-red.test.ts` (2026-08-22)
- [amount] printed “destroy this. If you do, the attack gets +X{p} where X is steam on this” locks `counters-on-source` from the pre-destroy source snapshot onto the if-you-do then-layer. Do not evaluate steam on the post-destroy GY object (0). Seat Teklovossen + Zero to Sixty boost — EVO090 / EVO091 / EVO092 — `packages/engine/src/rules/proposals/effects/if-you-do.ts` + `packages/cards/src/cards/EVO/actions/EVO090-hadron-collider-red.test.ts` (2026-08-22)
- [amount] printed “If the attacking hero has drawn 2 or more cards this turn” is `compare-amount` of `count` `cards-drawn-this-turn` `player: attacking-hero` ≥ 2, never `has-status the-attacking-hero-drawn-2-or-more-cards-this-turn`. Reads `playerCardsDrawn`. Sibling of grandfathered opponent-drawn-2. Snatch on-hit is one draw — two prior hits then the DR combat — DTD228 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/DTD/defense-reactions/DTD228-hold-the-line-blue.test.ts` (2026-08-22)
- [amount] printed “if they have more cards in hand than you” is `compare-amount` of `count` `cards-in-hand` opponent `gt` controller, never `has-status they-have-more-cards-in-hand-than-you`. 1v1 “Intimidate target hero” is `intimidate` `target: opponent` (Smash Instinct). Compare after intimidate. Seat Rhinar — PEN007 — `packages/cards/src/cards/PEN/actions/PEN007-lay-down-the-challenge-yellow.test.ts` (2026-08-22)
- [amount] printed “If this is the third time you've activated this ability” is `compare-amount` of binding `times-activated-this-ability` eq 3, never `has-status third-time-activated-this-ability`. Stamp usage+1 on announce/activate (limit usage after increment). Destroy a defending card is `player: any` combat-chain `defending: true`; combat {d} counts only defenders still on the chain (destroyed equipment must not keep {d}). Instant on the chain: `passBoth` then `closeCombat` — SEA012 — `packages/engine/src/procedures/activate-ability/stages/payment.ts` + `packages/engine/src/rules/rules-evaluator.ts` + `packages/cards/src/cards/SEA/actions/SEA012-palantir-aeronought-red.test.ts` (2026-08-22)
- [target] printed “Destroy up to 3 Toughness tokens” is at-resolution `up-to` 3 named Toughness Token, never `declared: on-stack` (announce has no token targets). Create Might is `destroyed-this-way`. `untilIdle({ entityTargets: "maximum" })` takes the full pool — SUP088 — `packages/cards/src/cards/SUP/instants/SUP088-rip-up-their-virtues-blue.test.ts` (2026-08-22)
- [trigger] printed “When this hits, sharpen this sword” is a granted `hit` trigger (`observes` source `attack`). Combat-damage emits `hit` only when remaining {p} > 0. Two Nimblism (2{d}+2{d}) vs Durendal 3+1{p}+2 is a hit, not a miss — MPW026 — `packages/engine/src/procedures/combat/combat-damage.ts` + `packages/cards/src/cards/MPW/attack-reactions/MPW026-sharpening-sparks-red.test.ts` (2026-08-22)
- [trigger] printed “When this leaves the arena during your turn” is leave-arena plus `turn-player` `who: self` on the triggered static, never a simultaneous-order harness gap. Snatch on-hit + destroy-this drains with `closeCombat({ ordering: "listed" })` — FLR012 — `packages/cards/src/cards/FLR/actions/FLR012-arcane-cussing-red.test.ts` (2026-08-22)
- [history] “if you've controlled a Vigor/Might this turn” is `control-object` `per:"turn"` reading turn-history flags stamped at create/destroy/leave-arena/turn-start — HVY058 — `packages/engine/src/rules/evaluation/conditions/control-object.ts` (2026-08-21)
- [binding] play-card pitch may generate chi from Inner Chi; Chi riders read `pitched-this-way-chi-card`. “effects don't trigger if an attack hits you” is restrict-trigger `attack-hits-you` — MST080 / MST079 — `packages/engine/src/procedures/play-card/payment.ts` + `packages/engine/src/rules/trigger-prevention.ts` (2026-08-21)
- [combo] printed Combo is `keywords:[combo]` plus one last-attack resolution grant. Extra +{p} on Gustwave seated on Benji is CRU047-a2 (first AAC hit → next attack +1), not combo applying twice. Isolate on Bravo (Open the Center). — KSU016 / KSU024 — `packages/engine/src/rules/evaluation/conditions/last-attack-this-combat-chain.ts` + `packages/cards/src/cards/CRU/heroes/CRU047-benji-the-piercing-wind.test.ts` (2026-08-21)
- [combo] “if Hundred Winds was the last attack … +1{p} for each other card named Hundred Winds on this combat chain” is `last-attack-this-combat-chain` plus `cards-in-zone` combat-chain `name` + `hasStatus: other-than-source` (not `other-than-self`, which is fail-closed). Combo printed names match through `matchesFilter` identity, not raw catalog slugs — EVR041 / EVR042 / EVR043 — `packages/engine/src/rules/evaluation/conditions/last-attack-this-combat-chain.ts` + `packages/cards/src/cards/EVR/actions/EVR041-hundred-winds-red.test.ts` (2026-08-22)
- [combo] printed Combo is `keywords:[combo]` plus conditional grants; unprinted go-again/dominate on the card make combo unconditional — WTR096 / WTR106 — grant-property on self (2026-08-21)
- [filter] “next attack that targets a Guardian hero” is appliesTo.next hasStatus `targets-a-guardian-hero` reading the defending hero's Guardian supertype (same table as targets-arakni) — SUP167 — `packages/engine/src/rules/evaluation/matches-filter.ts` (2026-08-21)
- [latch] “until the start of your next turn, attacks that target you have -N{p}” is unbounded `appliesTo.next` `count:all` `events:attack` with filter `targeting-you` (defending seat is the effect controller). `targeting-you` observes opponent attacks. Do not snapshot the combat chain at resolution — EVR160 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/EVR/actions/EVR160-this-round-s-on-me-blue.test.ts` (2026-08-21)
- [latch] “whenever an attack hits a hero this turn” after a resolving Action is resolution `delayed-trigger` windowed `this-turn` `matching:every`, not a source-static that dies in GY. “First AAC they play during their next turn” is `appliesTo.next` + `until-end-of-their-next-turn` (this-attack dummy observes opponent). “Next attack this combat chain” stays `appliesTo.next` `this-combat-chain` and is proven with `advanceUntil({ stopAt: "resolution" })`, not `closeCombat` — ELE151 / EVR094 / CRU109 / OUT071 — `packages/cards/src/cards/ELE/actions/ELE151-ice-quake-red.test.ts` (2026-08-22)
- [contract] printed contract tasks are `contract-task` `completeOn` + `filter` (cost/typeBox/hasKeyword), never prose-only `task` strings. Completing reads the seated filter against the banish/hit event object — DYN127 / DYN139 / DYN142 / DYN145 / EVO236 — `packages/engine/src/rules/contract-progress.ts` + `packages/cards/src/cards/DYN/actions/DYN127-rob-the-rich-red.test.ts` (2026-08-22)
- [filter] “if it attacks a Light hero, it gains +N{p}” is `appliesTo.next` `hasStatus: attacks-a-light-hero` (attackTargetHero + Light talent) plus `attacksOf: { binding: "it" }` on the chosen card — not a Spew-resolution has-status. — MON212 / MON214 / CHN024 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/MON/actions/MON212-spew-shadow-red.test.ts` (2026-08-21)
- [amount] printed “if this has {p} greater than the revealed card” is `compare-amount` subject-property current power vs `reference` `it` power (reveal `outputBinding`), not a power-greater-than status slug — SEA198 / SEA199 / SEA200 — `packages/cards/src/cards/SEA/actions/SEA198-crash-down-the-gates-red.test.ts` (2026-08-21)
- [amount] “+1{p} for each Light card charged this way” is `count` `charged-this-way` with a Light typeBox filter; play-time charge stamps the cohort on the play layer. This-way amounts lock at generation (CR 6.2.2b) even without `appliesTo`. Printed “this combat chain” expiry uses combatNumber -1 (live while combat is open; dropped at chain-close), not chainLinkNumber 0 — BOL009 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/engine/src/procedures/play-card/charge.ts` + `packages/engine/src/rules/continuous/runtime.ts` (2026-08-21)
- [combo] “if a Draconic attack was the last attack” is `last-attack-this-combat-chain` `filter.typeBox` Draconic+Attack, never `names:["Draconic Attack"]`. Grow Wings go again is only that grant (no `keywords:[goAgain]`) — HNT087 / HNT088 / HNT089 / HNT090 / HNT072 / CIN013 / CIN024 — `packages/engine/src/rules/evaluation/conditions/last-attack-this-combat-chain.ts` + `packages/cards/src/cards/HNT/actions/HNT087-grow-claws-yellow.test.ts` (2026-08-21)
- [if-you-do] prefix-flush of “Do X” before a then-branch decision must rewrite the live layer from `if-you-do` to Y (not only true `sequence` nodes). Otherwise X re-runs on resume — UPR140 frost + discard, HNT072 play-it — `packages/engine/src/procedures/layer-resolution/sequence.ts` (2026-08-21)
- [latch] Fletch “if it has an aim counter, it gains …” is a next-arrow grant with `hasCounter:aim`, not a self-static on the Fletch — OUT110 / OUT105 — `packages/cards/src/cards/OUT/actions/OUT110-fletch-a-yellow-tail-yellow.test.ts` (2026-08-21)
- [wager] an attack reaction that wagers the targeted sword uses the open chain-link attack (default when `attacker` is omitted, or `attacker: this-attack`), never the reaction layer source — MPW042 / MPW043 / MPW044 — `packages/engine/src/rules/proposals/mechanic-effects.ts` (2026-08-21)
- [amount] “if you've pitched a blue card this turn” is occupancy `pitch-zone-has` blue (pitch empties EOT). “yellow card in your pitch zone” is the same occupancy primitive. “defending hero has cards in soul” is `zone-count` soul `defending-hero` (fallback to the non-controller seat before Defend). “played as chain link 3+” is a while-static `chain-link-count`. “played or activated an attack reaction this chain link” is a while-static `attack-reactions-this-chain-link` count. Instant-this-link Overcharge is a while-static `played-this` Instant. Hit a marked hero with a dagger splits `hasStatus: marked` onto the hero target and type-box onto the hitting object — MST014 / DTD097 / DTD181 / CRU065 / OUT018 / PEN243 / HNT198 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/engine/src/rules/trigger-matcher.ts` (2026-08-21)
- [evo] printed “transform it into this, then equip this” targets the equipped Base+seat, not `self` of the resolving Evo. Occupied-seat throws are that self encoding — TCC009 / TCC010 / EVO034 / EVO037 / EVO049 — `packages/cards/src/cards/TCC/equipments/TCC009-evo-scatter-shot-blue.test.ts` (2026-08-21)
- [evo] “Destroy a card under this” is destroy `from: "under-this"` scanning hosted sub-cards (CR 3.0.14), never `hasStatus: under-this` on permanents. Quote uses object `zones:["under"]` of the activating host; legalTargets/scan read `subcardsByHostId` — EVO035 / EVO036 / EVO046 / EVO047 / EVO048 — `packages/engine/src/runtime-targets.ts` + `packages/cards/src/cards/EVO/equipments/EVO035-evo-engine-room-yellow.test.ts` (2026-08-21)
- [activate] discard-N activation costs parse like banish (count ≥ 1). An exact-count pool (CR 1.8.6c) is determined and auto-bound so any-hero pays from the activator's hand. Destroy-self from arsenal is `functionalZones:["arsenal"]` plus existing `face-up-in-arsenal`. “Any hero may activate” is `activatableBy:"any-hero"`, not a status slug. Next-arcane prevent is `prevention` `times:1` `damageType:arcane` — MON000 / SUP257 — `packages/engine/src/procedures/activate-ability/stages/declarations.ts` + `packages/cards/src/cards/MON/actions/MON000-great-library-of-solana.test.ts` (2026-08-21)
- [binding] “if a hero has 2+ yellow in pitch, they gain +1{i}” is for-each each-hero plus zone-count `player: iteration-subject`, not a global any-hero count targeting unbound `it`. zone-count/history player switches include iteration-subject — MON000 — `packages/engine/src/rules/evaluation/helpers.ts` (2026-08-21)
- [binding] choose-card `this-attack` must keep `it` as the exact-attack binding (weapon proxy). Do not overwrite `it` with a plain object snapshot — that leaks the −{p} / lose-keyword latch onto the next swing — MPG128 — `packages/engine/src/rules/proposals/effects/choose-card.ts` (2026-08-23)
- [binding] hit “banish the top; if it's red, repeat once” stamps banish `outputBinding: "it"` (not `"banished"`) so `binding-matches` on `it` can fire the repeat — PEN145 / PEN146 — `packages/cards/src/cards/PEN/actions/PEN145-excessive-bloodloss-yellow.test.ts` (2026-08-22)
- [latch] Ice “cards and activated abilities cost opposing heroes an additional {r} this turn” is `target: opponent` + unbounded `appliesTo.next` `events:["play","activate"]`. Do not snapshot the opponent’s stack at Attack resolution. Frost Lock is unconditional; Cold Wave is fused-only — ELE035 / ELE038 / ELE039 / ELE040 — `packages/cards/src/cards/ELE/actions/ELE035-frost-lock-blue.test.ts` (2026-08-21)
- [combat] Tower “non-equipment cards get -1{d} while defending this” is a while-static on this-chain-link, not an Attack-resolution defender scan — TCC035 — `packages/cards/src/cards/TCC/actions/TCC035-lay-down-the-law-red.test.ts` (2026-08-21)
- [combat] printed “Attack action cards have -1{p} while defending this” is the same while-static + `this-chain-link`, never resolution `duration: while-condition`. Hit-to-soul stays a triggered static. Combo + chain-link-3 go again is one resolution plus a while-static, not two resolution abilities — MON008 / MON009 / MON010 / SUP246 — `packages/cards/src/cards/MON/actions/MON008-herald-of-triumph-red.test.ts` (2026-08-22)
- [combat] Illusionist “auras you control are weapons … Attack” is grant type Weapon + granted `attack-with` `self`. Prove with `activateAttack` on the aura (drain to Defend); bare `activate()` leaves combat closed. Combat-chain scans match the active attack by instance id when incarnation differs. “Illusionist attacks” is Illusionist supertype, not Attack subtype (auras stay in arena) — DTD216 / MON003 / ENG002 — `packages/engine/src/rules/evaluation/targets/object.ts` + `packages/cards/src/cards/DTD/weapons/DTD216-reality-refractor.test.ts` (2026-08-21)
- [play] Blood Debt is only the end-phase life loss (CR 8.3.11). Play from banished is `staticKind:"play"` `fromZones:["banished"]` (CHN013 / IAR112). `played-from-banished-zone` reads object-history any move from banished, not only the last (stack → chain clobbers origin). Unprinted go-again/overpower on the module make the rider unconditional. Printed “if an opposing hero has 1 or more cards in their soul, you may play this from banished” is that play-static plus `zone-count` soul `player:opponent` ≥ 1, never `opposing-hero-has-cards-in-soul`. IAR164 still needs the play-static even when the rider is only “if played from banished” — IAR174 / IAR175 / IAR176 / IAR164 / DTD172–174 — `packages/engine/src/rules/legality/play.ts` + `packages/cards/src/cards/CHN/actions/CHN013-rip-through-reality-red.test.ts` (2026-08-22)
- [play] “Banish … You may play them this turn” is a `play-card` duration grant on the banished cohort (`this-turn`), not an `optional` immediate play. Multi-object banish `outputBinding` must stamp the whole cohort (array), not the last card. Direct play-card grants already authorize banished via play.ts — PEN277 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/PEN/actions/PEN277-tome-of-pandemonium-yellow.test.ts` (2026-08-22)
- [play] printed “You may play it this turn” after search/banish/surge is that same duration grant — never wrap `play-card` in `optional` (the boolean is a false second may; accept used to fail-loud as unmigrated). Self-banish must `outputBinding: "it"`. — ASR003 / EVR037 / DTD106 / ROS167 / IAR222 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/equipment/iris-of-the-blossom.test.ts` (2026-08-30)
- [filter] printed “with different names” is an entity-target SET constraint on `printedIdentityKey` (pitch-stripped printed name). Two Snatch printings share a name; distinct-name count < N reverses the activation. Never compare canonical ids. — DTD106 — `packages/engine/src/kernel/different-names.ts` + `packages/cards/src/cards/equipment/spoiled-skull.test.ts` (2026-08-30)
- [play] if-you-do hand-banish then “play it this turn” must grant the `banished-this-way` cohort, not leftover `it` (the attacking source). Banish stamps that cohort; play-card this-turn prefers it. Harness `playInstance` resolves canonical zone ids. — DYN053 / UPR054 / UPR076 / UPR081 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/DYN/actions/DYN053-blessing-of-qi-red.test.ts` (2026-08-22)
- [play] printed “if this is played during an opponent's turn” is `turn-player` `who: "opponent"` (active seat ≠ controller), never `hasStatus: played-during-opponents-turn`. Own-turn Actions go to GY; the Action is unplayable without an Instant window — EVR134 / EVR135 / EVR136 — `packages/engine/src/rules/evaluation/conditions/turn-player.ts` + `packages/cards/src/cards/EVR/actions/EVR134-timekeeper-s-whim-red.test.ts` (2026-08-21)
- [play] Specialization is an in-match play gate (CR 8.3.7 is not deckbuilding-only): quote reverses unless the controller hero's names contain the keyword `hero`. “Play only if you have played 6+ blood-debt this turn” is a non-optional play-static `played-this`; do not put that condition solely on `optional` `fromZones:["banished"]` (optional permissions skip the play-condition scan) — MON189 / MON190 — `packages/engine/src/rules/legality/play.ts` + `packages/cards/src/cards/MON/instants/MON189-doomsday-blue.test.ts` + `packages/cards/src/cards/MON/instants/MON189-doomsday-blue.test.ts` (2026-08-22)
- [latch] Dead Eye aimed “when this hits, look at their hand and they discard it” is a next-Arrow grant with `hasCounter:aim` (Fletch), not a self-static on Dead Eye — DYN155 — `packages/cards/src/cards/DYN/actions/DYN155-dead-eye-yellow.test.ts` (2026-08-21)
- [history] Draconic chain-link count stamps when the Attack Step opens if the attack’s type box has Draconic as a **type or supertype** (catalog talents often live in `types`) — UPR050 — `packages/engine/src/rules/reducers/combat.ts` (2026-08-21)
- [cost] Combo “this costs {r} less to play” is a play-static `cost-reduction`, never a resolution `modify-numeric` on cost (too late; play already paid). Isolate power on a Ninja without Benji’s first-AAC +1 — OUT076 / KAT008 / KAT018 — `packages/cards/src/cards/OUT/actions/OUT076-descendent-gustwave-blue.test.ts` (2026-08-21)
- [harness] “the crowd cheers you” already emits `crowd-cheers`; hero “whenever the crowd cheers you, create a token” is a triggered layer that `passBoth` will not flush. Drain with `resolveUntilIdle` — SUP034 / SUP002 — (2026-08-21)
- [latch] “Earth, Ice, and Elemental action cards have +1{d} while defending this turn” is unbounded `appliesTo.next` `events:["defend"]` `count:all`, not an at-resolution combat-chain scan of currently-defending cards — ELE114 — `packages/cards/src/cards/ELE/defense-reactions/ELE114-pulse-of-isenloft-blue.test.ts` (2026-08-21)
- [contract] printed “You are contracted to banish opponents' [filter]” is `contract-task` `completeOn:"banish"` plus `FabCardFilter`, never regex on `task`. Completion is a controller-acted `banish` of an opponent-owned match (CR 8.5.39a); pre-seated matching cards do not complete. Hit-named-marked-hero is `completeOn:"hit"` + `moniker` + `hasStatus:marked` (pre-hit `targetWasMarked`). Printed “cards with the chosen name” is `hasStatus: named-card` (name-card string + hero `named-card:<name>` stamp), never `name:"chosen"` (that is choose-card object identity). — DYN127–129 / DYN139–147 / EVO236 / SUP245 — `packages/engine/src/rules/contract-progress.ts` + `packages/cards/src/cards/DYN/actions/DYN127-rob-the-rich-red.test.ts` (2026-08-22)
- [keyword] printed “When this hits, it gets go again” / “If you do, this gains go again” / “if you have more {h} than them, this gets go again” / play-time “if you control a Spectral Shield, this gains go again” is grant-property on the rider, never `keywords:[goAgain]`; AP legs prove hit/no-shield vs hit with AP deltas. Ledger pins written against a defect must flip when the module does, or they re-arm it — FAI014 / CRU066–068 / CRU070–071 / IRA006 / DTD066–068 / PEN303–305 / DYN224–226 / PEN202 — `packages/cards/src/cards/DYN/actions/DYN224-spectral-prowler-red.test.ts` (2026-08-26)
- [keyword] printed “if it scrapped a card, this gets overpower” / High Tide “+1{p} and overpower” / “if you control a Spectral Shield, this gains overpower” is grant-property, never `keywords:[overpower]`; prove the negative side with notToHaveKeyword plus a legal two-action-card `defendWith([a, b])` — EVO102–104 / SEA134–136 / DYN227–229 — `packages/cards/src/cards/EVO/actions/EVO102-hydraulic-press-red.test.ts` (2026-08-26)
- [keyword] printed “if you played or created an aura this turn, this gains dominate” / soul-banish “your attacks this turn get dominate” is grant-property, never `keywords:[dominate]` — EVR110–112 / DTD010 — `packages/cards/src/cards/EVR/actions/EVR110-drowning-dire-red.test.ts` (2026-08-22)
- [filter] attack observes `filter.name` uses the printed full name (`"Suraya, Archangel of Erudition"`). `namesMatch` compact-strips punctuation so i18n commas match slug-derived names. `activateAttack` forwards `optionals`/`entityTargets`. Soul-banish is the family proof; on-stack any-target arcane may still need explicit `.target` after accept — DTD005–012 / MON219 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/DTD/allies/DTD005-suraya-archangel-of-erudition.test.ts` (2026-08-22)
- [trigger] delayed “whenever Dawnblade hits a hero” is observe-attack `nameContains` plus `target.kind:hero`. `filterAppliesToHeroTarget` must not steal a nameContains-only filter onto the defending hero — DVR008 — `packages/engine/src/rules/trigger-matcher.ts` (2026-08-23)
- [filter] printed “when this attacks Arakni” is observe `moniker:"Arakni"` plus `target.kind:hero`. `filterHasCardIdentity` must include `moniker`/`nameContains` so `filterAppliesToHeroTarget` matches the defending hero, not the attack card — CIN015 — `packages/engine/src/rules/trigger-matcher.ts` (2026-08-22)
- [harness] Instant on the attacking card needs attacker priority. `playAttack({ stopAt: "on-attack" })` lands at defend-pending (no holder). Use `toReaction()` then `activate` then `passBoth` then `choose("modify-numeric")`. `{t}/{u} a cog` is activation tap/untap cost (Rust Belt sibling) — SEA024–029 / SUP254 — `packages/engine/src/procedures/activate-ability/costs/parse.ts` (2026-08-22)
- [cost] printed “{r}, destroy this, charge your hero's soul” is a mixed activation cost: resources + destroy-self + `chargeTargets` (hand → soul + `charge` event). Charge is not play-only additional-cost. AR Instant: `toReaction("attacker")` then activate, auto-bind unique charge/attack — DTD060–062 — `packages/engine/src/procedures/activate-ability/costs/parse.ts` (2026-08-22)
- [clash] printed “If there is a winner, the other hero puts a -1{d} on a [slot] they have equipped. If they don't, they lose 1{h}” is clash then `or` of `won-clash`/`opponent-won-clash`, then `control-object` `player:loser` on the equipment seat else lose-life `who:loser`. Never `clash-has-winner` or `attack-target`. Equipment is `equipment-head/chest/arms/legs` or `weapon` (Off-Hand), not arena `permanent`. `zoneObjectMatchesPlayer` reads winner/loser bindings — MPG047–051 — `packages/engine/src/rules/evaluation/helpers.ts` + `packages/cards/src/cards/MPG/blocks/MPG047-clash-of-heads-yellow.test.ts` (2026-08-22)
- [replacement/damage-source-filter] kernel/replacements/collect.ts eventMatchesPattern → damage-family (deal-damage/dealt-damage/prevent) replaces.filter evaluates data.source (DamageEventData), with candidate.controllerId passed as abilityControllerId → Frazzle RGB + PEN202 AAA; controller-effect = FILTER_STATUS_HANDLERS scope check (2026-08-23)
- [harness] attackToDefendInternal overrode explicit play targets with the default defender — usurp auras are reachable via play(attack, {target: auraInstanceId}); the engine quote/classification was already correct — IAR154/156/157 — packages/engine/src/testing/test-engine.ts (2026-08-23)
- [dispatch] play-event event-and-state triggers resolve grant-property-ABILITY effects only; modify-numeric, deal-damage, and keyword grants from those triggers are not dispatched (crossing green, aether-slash/deathly-duet/torque-tuned pinned with stamps/history verified true) — residual owner: trigger scheduling in the play transaction — packages/engine/src/procedures/play-card/finalize.ts stamp sites + pinned suites (2026-08-23)
- [fixture] play-event rider inertness was mostly MISSING resourcePoints in the suite: with cost unpaid by the declared pitch, auto-pitch silently pays and pitchedInstanceIds stays empty — the play bindings then carry no pitched-type stamps. Always seed RP so the declared pitch is the actual payment — DYN176-178 (Deathly Duet full-real 9/9) — pinned suites DYN182-184 / EVO114-116 (2026-08-23)
- [dispatch] create-token and modify-numeric DO dispatch from play-event event-and-state triggers; deal-damage does NOT (aether ×3) and while-static grant-property KEYWORD onto the chain-attack self does not project (torque ×3; modify-numeric on other objects works per ratchet) — engine/play-event-deal-damage-inert + engine/while-static-self-keyword-projection (2026-08-23)
- [fixture ×3] the deal-damage 'dispatch gap' was ALSO fixture payment: declared pitch cards are only consumed when resourcePoints cannot cover the cost — seed RP 0 so the pitch IS the payment. Aether trio full-real 9/9 (arcane + hit both land) — DYN182-184 (2026-08-23)
- [projection] grant-property KEYWORD onto the chain-attack self is inert in ALL encodings (while-static, play-event event-and-state trigger, resolution-conditional) while modify-numeric / create-token / deal-damage riders dispatch fine — engine/keyword-projection-onto-chain-attack ×3 (torque-tuned, history proven true) (2026-08-23)
- [projection-fixed] keyword grants onto the chain-attack self DO project from attack-event event-and-state triggers (EVO104 hydraulic-press shape: evt "attack" + observes source attack + any state half incl performed-this-turn) when the drive runs the FULL auto-drain — stopAt-based partial drains leave the trigger resolution pending and read as inert. Torque trio full-real 9/9 — EVO114-116 (2026-08-23)
- [order] crowd latch riders: a resolution-LAYER condition on been-booed/cheered-this-turn evaluates BEFORE the attack-event trigger's crowd effect commits (stamp lands during the attack layer, after play resolution) — re-encode the +N{p} rider as a static triggered on the crowd-boos/cheers EVENT; stamp and grant share one commit window — SUP090-092 / SUP027/028 / APS017 full-real 9/9 each (2026-08-23)
- [audit] before pinning "fires when blocked": recompute the block arithmetic — a 3-block vs 4{p} is a HIT (damage 1). The hit event is correctly damage-gated in combat-damage.ts (damage > 0). PEN306-308 boundary corrected to a true 4-vs-4 full block — engine/next-hit-latch-fires-when-blocked was a false family (2026-08-23)
- [stop-rule] unmigrated-reveal-choose (pry): reveal proposal IS supported and events commit, but the sequence continuation drops the optional choose-card+move+draw steps after the event-committing reveal step (choose-card never reached) — same class as §5 HNT255 iterations-dropped. freeze (cold-snap): play-declaration precheck rejects on-stack targets whose player:target-controller binding only materializes during resolution — fix belongs in declarations.ts skipping deferred-binding requirements. Both parked for the closer after two rejects (2026-08-23)
- [audit ×2] before recording an effect-inert family: probe with a NON-EMPTY target zone — intimidate on an empty hand correctly no-ops (PEN306-308 un-pinned real; hand 3→2 banish verified) (2026-08-23)
- [gate] banish until:'while-in-arena' with a source is LIVE (card-movement-effects: gate widened + leave-arena delayed-trigger watcher registers and pattern-matches) — entry banish real (TP trio 9/9). The watcher's fire-time RETURN binding (banishedUntilSourceLeaves) does not survive to the resolution layer — engine/banish-until-source-leaves-return-binding ×3 (2026-08-23)
- [identity] banish-until-source-leaves: the fire-time binding must carry the POST-banish identity — banish resets incarnation and the watcher fires in a later batch, so matcher reanchoring cannot fix a stale cross-batch ref. Bake nextFabDestinationRef(state, object, banishResetOffset) into the register event's binding — TP return real end-to-end (2026-08-23)
- [audit ×3] before recording a 'mechanic-unsupported' family: check the MODULE carries the full printed text — high-roller only encoded the conditional instead-branch (roll+intimidate base missing) while the engine's roll machinery (effect, history, evaluators) was already complete. Re-encoded sequence [roll, conditional] — trio real 9/9 (2026-08-23)
- [gate] declarations precheck: on-stack object targets with deferred player bindings (target-controller/iteration-subject) escape ONLY when the pre-play candidate count is insufficient — countable targets keep their play-time declaration (singe regression caught in CI) — cold-snap trio real / conquer trio written (2026-08-23)
- [residual] freeze registers a rule-atom but stamps no observable hasStatus:"frozen" on the card, so "destroy a frozen card" filters find no candidates — engine/frozen-status-not-observable ×3 (pinned with the freeze driven via cold snap) (2026-08-23)
- [gate] a conditional grant nested as a static-play ability's `effect` never evaluates — the printed keyword leak MASKED it. Grants land only via the PEN202 shape: separate `kind:"resolution"` ability with top-level `condition` + `grant-property`. Producer side for banish-cost clauses = play-card/payment.ts costBindings from the `paid` snapshot talents (`shadow-card-banished-this-way` added alongside the power6 family); evaluator side = has-status.ts binding-reader + `thisWayHasTalent` fallback over ["banished-this-way","them","it"] (ram-raider / consuming-aftermath trios real, arc-ramp boundaries real, 25 cases) (2026-08-26)
- [audit] 'X has KEYWORD if <condition>' entries must not carry the keyword in keywords[] — drill-shot RGB: the continuous has-counter grant was already live end-to-end, so stripping the printed leak flips the pin boundary real with zero engine work (2026-08-26)
- [gate] a follow-up clause referencing this card ("whenever it deals damage") must NOT be encoded as observes event-object + literal name filter ("It" never matches) — use observes kind:"source" + selector:"damage-source" (subject:self); armed-until-triggered riders resolve while the chain is OPEN, so the provable printed result is the same-chain hit getting +1{p}, never a cross-chain carryover (explosive-growth RGB 9/9 real) (2026-08-26)
- [gate] 'when THIS is put ...' move-zone triggers must scope moved-object via observes kind:"source" and express orientation through the event's faceDown CONSTRAINT (move-zone exposes faceDown in triggerEventSpec constraints) — relationship:"any" + hasStatus face-up filter fires once per OTHER card put too, multi-put drivers (silver-the-tip cohort) stack it per-put (head-shot RGB / dry-powder-shot 12/12 real) (2026-08-26)
- [audit] before recording an 'engine violates keyword CR' family: quote the LIVE CR definition — Ward N (CR 8.3.20) has NO arcane qualifier and covers any damage; only Spellvoid 8.3.x/Arcane Barrier say "arcane". Ward destroy-on-combat-hit was already correct; ward family closed as false diagnosis, suites un-pinned with the verbatim rule cited (2026-08-26)
- [audit ×2] before recording OR trusting a 'mechanism never binds' pin, check the FIXTURE is in the clause's filter scope — crush-the-weak pins used a 4{p} Snatch against a ≤3-base lock; restrict-play rule-modification was fully live (dustup denied via playRestriction). Denial proofs: expectFabUnplayable(() => playAttack(x), /restricts this object from being played/) after rolling into the locked player's action phase. Also re-kind mis-homogenized family members (confidence=ability-strip, star-struck=damage-scaled require) into their own rows (2026-08-26)
- [gate] an effect step with outputBinding MUST stage the binding on its proposed event or every follow-up "If it has…"/binding-target condition sees nothing — proposeSharpen lacked what look/reveal already did; add bindings:{...layer.bindings,[outputBinding]:object} (flurry rider went 0→1 real). Scan OTHER proposal emitters for declared outputBinding handling as a class of silent-dead follow-ups (2026-08-26)
- [latch] printed “its/your next attack WITH IT this turn gets X / costs {r} less” on a non-attack card layer must latch the bound sword — `target:{selector:"binding",binding:"it"}` + `appliesTo:{attacksOf:true,next:{typeBox Weapon}}`, never `selector:"this-attack"` (objectTargets returns null with no open chain ⇒ silent no-op grant). modify-activation-cost omits `events` (engine pins that discriminant to ["activate"]); keyword/ability grants carry `events:["attack"]`. Prove discounts with remaining-RP deltas, not activation-rejection probes (unpayable activations open a payment decision rather than hard-fail) — PEN054–056 / AHA013 / AHA018 / AHA023 — packages/cards/src/cards/PEN/actions/PEN054-cut-n-carve-red.test.ts (2026-08-26)
- [shuriken] printed "{r}, {t}, destroy this when the combat chain closes: Attack" is CIN002 composition (cost {r}+{t}; effect sequence[attack-with, delayed-trigger combat-chain-close actor/observes none until-triggered → destroy self]) — an immediate `destroy-self` cost removes the Item before combat opens. Members: CIN002 ✓, CRU051 ✓, OMN232/233/234 re-encoded + AAA trios (2026-08-26) — packages/cards/src/cards/OMN/instants/OMN233-razor-ring-blue.test.ts
- [harness] under FAB_MANUAL_HARNESS the entry-crank choice resolves without stamping cranked-this-turn; use the partial `{ autoPassPriority: false, autoPitch: false }` from keyword-crank for crank-gate cards — EVO006 banksy trio (2026-08-26)
- [harness] a played cost-0 Item still needs its priority passes (or untilIdle) before entry counters exist and the arena seat shows; item plays consume no action point while .activate() on weapons/items does not land you in the Defend Step like playAttack does (advanceCombatTo("defend") or activateAttack + immediate defendWith) — OMN233/234/AJV002/APS003/EVO006 trios (2026-08-26)
- [material] printed “While this is under a permanent other than <Name>” composes the existing hosting topology — condition `source-is-subcard-of-host` + optional `hostOtherThan` (compare host base names; `current` is unprojected at early atom stages), grant target selector "host" — the reconciler whitelist for under-zoned statics expects exactly this condition. Materials seat arrange-only via the test `hostUnder` verb (construction layout); hosted statics register only inside committed transactions, so drive one ordinary public play before asserting. Miragai's own strip currently over-reaches while merely in play — pairing legs against her are unprovable until engine/miragai-phantasm-strip-global-overreach closes — DYN002-004 / EVO246 / MST235 / ROS252 — packages/cards/src/cards/DYN/actions/DYN002-dust-from-the-golden-plains-red.test.ts (2026-08-26)
- [modal] printed “Choose any number” declares its modes AT PLAY DECLARATION via `play(card,{modeIndexes:[…]})` (CR 1.7.5 modes enter with the layer) — there is never a pending option decision afterwards; assert each leg's own rule-visible outcome (token removed/created). Printed “Choose 1 or more” has no at-least-one quantifier on the type surface, so `any-number` is the closest expressible encoding until one exists. Pitch-condition stamps (“pitched-attack-action-card-to-play-this”) read layer bindings committed at finalize — pass the pitch explicitly with `play(card,{pitch:[ref]})`; verify the pitched card really carries the Attack SUBTYPE rather than trusting its directory/class name — DYN174 / APS014 / SUP007 / PEN225 — packages/cards/src/cards/DYN/actions/DYN174-diabolic-ultimatum-red.test.ts (2026-08-26)
- [trigger] top-level `pattern.faceDown` on move-zone/banish/enter-arena triggers is enforced in matchesNormalizedTriggerEvent next to the random check (unflagged event = face-up); its ONLY catalog users are the four arsenal arrows ARC057-059 + SEA106, so play-path regressions can be upgraded from the silver-tip single-arrow happy to a true face-down reload case later — HEAD-of-lane note (2026-08-26)
- [policy] printed "whenever it deals damage" re-fires within its window: use `policy:{kind:"windowed",duration:"this-chain-link",matching:"every"}` (ELE004 shape), never one-shot `until-triggered`; and next-printed riders carry `count:1` beside `attacksOf:true` because reconciler defaults attacksOf-without-count to all — ELE067-069 / PEN054-056 / AHA013/018/023 (2026-08-26)
- [play-bar] printed "<card> can't be played from hand" must be a doomsday-style gate — staticKind:"play" + role:"permission" + condition not(has-status played-from-hand) — never a hand-zoned rule-modification restrict (hand statics are never collected, so the rule simply does not exist at quote). The has-status handler reads source.zone at quote and declaration facts after the move. On-hit 'create a Frailty, Inertia, or Bloodrot Pox token under their control' composes choose-and-create-token options[frailty,inertia,bloodrot-pox] chooser/controller attack-target; drive via arsenal playAttack → resolveUntilIdle({effectResolution:'frailty'}) answers the defender's closed-list choice — OUT162–164 / OUT169–170 precedent-gap — packages/cards/src/cards/OUT/actions/OUT162-death-touch-red.test.ts (2026-08-26)
- [stale-audit] before building engine fixes for an "inert rider" family, drive ONE probe through the printed scenario first — half the batch-budget era rows describe behavior that already works at head (dagger-hit lose-life drained correctly for every member; OUT142's X legitimately counts zero absent a prior same-combat dagger link; HNT012 kiss-of-death is the only authored Dagger AAC, making go-again chains the required proof shape since "this combat chain" spans links but NOT separate combats) — CIN017 / HNT186 / HNT187 / HNT007 / OUT142 — packages/cards/src/cards/CIN/actions/CIN017-poisoned-blade-red.test.ts (2026-08-26)
- [search] class-as-type prints (Guardian/Assassin/… living in types[], supertypeSets null) must be searched with a flat typeBox requiring the class alongside Action — nested supertypes/or-filters silently fetch nothing through matchesFabSnapshotFilter on these snapshots. Known trap (open): play-time declarations can pre-seed effectTargets[key]=[] for enter-arena search leaves, short-circuiting pool rescan into a mayFail empty commit — signature to grep is selected:[], keyVal:[] at proposeSearch — WTR047 Show Time! vs MON266 Belittle precedent-gap — packages/cards/src/cards/WTR/actions/WTR047-show-time-blue.ts (2026-08-26)
- [amp] the amp resolution leaf DOES dispatch on non-attack Action layers; the old "tangled cascade" was play()'s auto-DECLINE of optionals in its internal drain. Accept path: optionalBoolean:true + entityTargets:minimum after play(), then assert destroyed token vacates state.objects (use cardsIn length, not not.toBeIn which throws on absent records) and toHaveAP(1) for the granted refund — OMN100/101/102 — packages/cards/src/cards/OMN/actions/OMN100-arc-ramp-red.test.ts (2026-08-26)
- [look] printed “Target hero reveals N (or their hand on an opponent's turn); you may choose a card. Put it on the bottom of their deck. They draw a card” is Siren-shaped sequence: reveal `playerTarget: any-hero` + `playerTargetBinding: "looked-hero"` then `optional` choose-card `revealed-this-way` + move `it` + draw `player: "target-controller"`. Do not wrap Target hero in `for-each`+`iteration-subject` (proposeForEach drops the inner sequence). Conditional reveal counts unwrap via `unwrapSelectionCount` before CR 1.8.6c. findDecision must apply that playerTargetBinding to the at-resolution pool scan — default controller seat made Pry reveal the caster's hand. Hand-owner answers the reveal chooser; controller answers the optional. Staging look/reveal must call `objectTargets` with the real targetPath. `expectFabCard` does not search deck — assert `zone("deck")`. Own-turn exact-N=pool auto-binds; over-count the reveal chooser so choose-1 still opens. Opponent-turn Instant (Iyslander) is the instead-all + draw proof. Never `conditional.instead` for the all-hand swap — EVR128 / EVR129 / EVR130 — `packages/engine/src/rules/proposals/shared.ts` + `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/EVR/actions/EVR128-pry-red.test.ts` (2026-08-27)
- [crush] printed “destroy a Seismic Surge they control / destroy all equipment they control with -1{d}” is live crush `dealt-damage` gte 4 plus destroy `player: "attack-target"`. Defender-seeded WTR075 survives P1's action phase (self-destruct is the controller's start). Destroyed Surge tokens leave the game — assert `zone("arena")` occupancy, not `toBeIn("graveyard")`. “All -1{d} equipment” reuses Mangle's Equipment+hasCounter scan (fixture `defenseCounterTotal: -1`). “To a Guardian hero” is MPG027's observes Guardian type-box stolen onto the hero target — MPG076 / MPG077 / BDD014 / MPG028 / OMN087 — `packages/cards/src/cards/MPG/actions/MPG076-flatten-the-field-red.test.ts` (2026-08-27)
- [out-of-scope] SMP Event cards have no in-match reveal/resolution in the 1v1 product. Seated Event shells stay inert; do not invent an Event-deck primitive — SMP008–024 / SMP027 / SMP032 — `packages/cards/src/cards/SMP/events/SMP007-air-of-a-comeback.test.ts` (2026-08-27)
- [out-of-scope] Event Equipment “You may equip this” is that same Event-deck window, not a missing in-match equip primitive. Operative Instant/Action/Blade Break clauses stay 1v1-playable. — SMP014 / SMP017 / SMP018 — `packages/cards/src/cards/equipment/bloodied-helm.test.ts` (2026-08-30)
- [filter] typeBox tokens (Guardian class-as-type, Attack-as-type) live in types[] with null supertypeSets. matches-filter must union types/supertypes/subtypes so a tutor for Guardian AAC finds Show Time!/Flatten. Search of a mayFail enter-arena trigger is Belittle's `entityTargets:maximum` / `entityTargetCanonicalId`. Named-card follow-ups are `hasStatus: named-card`, never `name:"chosen"` — WTR047 / OMN028 / OMN029 / PEN182 / PEN301 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/WTR/actions/WTR047-show-time-blue.test.ts` (2026-08-27)
- [combat] printed “if this didn't hit” is about THIS object as the attack. lastAttackDidHit defaults false, so a defending card would mint the miss token at chain-close — skip sources in lastClosedDefendingInstanceIds and require live combat.attack === source — EVR002 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` (2026-08-27)
- [replacement] printed “whenever … would deal damage this combat chain, instead plus N” is a resolution `replacement` with duration `this-combat-chain`. Expiry must stamp combatNumber -1 (live while combat is open); chainLinkNumber expires when Layer (0) becomes Attack (1). “Whenever” must `consumptionPolicy: never` so a later same-chain Lightning still boosts. Follow-up links stay `advanceCombatTo("resolution")`; two boosts need `ordering: "listed"` — ELE186 / ELE187 / ELE188 — `packages/engine/src/rules/reducers/rules-stack.ts` + `packages/cards/src/cards/ELE/actions/ELE186-ball-lightning-red.test.ts` (2026-08-27)
- [cost] required “banish N from hand” is `isRequiredBanishHandPlayCost` (sibling of optional hand / required GY). Quote lists `effect-cost` and begin-play throws unmigrated without it. Payment already stamps `it` / `banished-this-way`. “If it's Earth / Lightning / Ice” is PEN202-shape resolution `binding-matches` on `it`, never `keywords:[goAgain, dominate]`. Pitch stamp `pitched-this-way-earth-card` was already live — seed `resourcePoints: 0` so the declared Earth pitch actually pays (Log Fall / Deathly Duet). Printed “target equipment” at-resolution needs `player: "any"`; default controller seat misses the opponent helm — PEN201 / TER018 / TER025 / ELE093 / PEN205 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/PEN/actions/PEN201-seeds-of-strength-red.test.ts` (2026-08-27)
- [combo] printed “while this is face-up in any zone and would gain {p}, instead that much plus 1” is `rule-modification` `mode: "amplify"` `action: "gain-power"` `subject: self` (apply-atom `powerGainAmplification`). Static `type:replacement` `replaces:gain` never compiles (semantic kind is replacement-or-prevention, not a CE producer) and kernel replacements never observe evaluator numeric grants. Combo last-attack is previous closed link — `resolveUntilIdle` after the setup attack closes the chain; drain `advanceUntil({ stopAt: "resolution", optionals: "decline", ordering: "listed" })` then play the combo card. Errata Bulletin #6 — BEN008 / BEN014 / OUT067 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/BEN/actions/BEN008-back-heel-kick-red.test.ts` (2026-08-27)
- [roll] printed “Roll a 6 sided die. Intimidate. If you have rolled a 4, 5, or 6 this turn, instead intimidate twice” is sequence roll then conditional on `rolled-a-4-5--or-6-on-a-die-this-turn` (reads `playerHighestDieRollThisTurn >= 4`). `lastDieFace()` after play proves 4–6 → 2 intimidates, 1–3 → 1. Do not seed a die-face enabler. MPW028 And Again is a different unhandled `attacked-with-target-this-turn` slug — EVR005 / EVR006 / EVR007 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/EVR/actions/EVR005-high-roller-red.test.ts` (2026-08-27)
- [create] printed “Hero abilities can't create cards” is restrict-create matching the creating layer source (`typeBox.types: Hero`), never `hasStatus: hero-ability`. proposeCreateToken / create-card skip the create event (empty events) so the rest of the layer still resolves. Enter-with-N-counters + action-phase-start unless/remove was already live — ARC007 / ARC035 / CRU075 / SEA252 — `packages/engine/src/rules/create-restrictions.ts` + `packages/cards/src/cards/SEA/actions/SEA252-preach-modesty-red.test.ts` (2026-08-27)
- [cost] printed “banish 1 or more from hand rather than pay {r}” is play-static `alternative-cost` `count:{type:"any-number"}` `min:1`. Quote + payment reuse `isRequiredBanishHandPlayCost`; the any-number chooser min is `cost.min` (empty hand reverses). Blood-debt refund is `count` `banished-this-way` `hasKeyword:blood-debt`. Unprinted `keywords:[goAgain]` made Soul Reaping / Big Horn go-again unconditional — strip it. Big Horn is while-static `defended-by-fewer-than-2-non-equipment-cards`. Play-from-banished grant is already `played-from-banished-zone`; search-to-permanent already seats auras. `playAttack` Pay/Decline is `optionals:"decline"`; on-attack search is `optionals:"accept"` `entityTargets:"pause"` then `.target(cardIn("deck"))` then `advanceUntil({stopAt:"defend"})` — CHN008 / IAR112 / CRU010–012 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/engine/src/procedures/play-card/declarations.ts` + `packages/cards/src/cards/CHN/actions/CHN008-soul-reaping-red.test.ts` (2026-08-27)
- [binding] printed “Whenever you play a Crouching Tiger this turn, name a card. It gets that name” is resolution `delayed-trigger` windowed `this-turn` `matching:every` (source-static dies in GY). “Whenever you play an AAC with ephemeral” on an Attack stays a chain-functional source-static. Grant-property name `"chosen"` targets binding `it` (the played card), never `self` of the resolving Action. `playAttack(..., { stopAt: "on-attack" })` then `.choose(printedName)`. Kayo play-AAC-6{p} roll+halve/double is already live (`lastDieFace`). Sow Tomorrow GY-bottom+self-banish was already live — MST052 / PEN264 / CRU002 / ELE140 — `packages/engine/src/rules/proposals/effects/name-card.ts` + `packages/cards/src/cards/MST/actions/MST052-shifting-winds-of-the-mystic-beast-blue.test.ts` (2026-08-27)
- [freeze] printed “Target hero may pay {r}… If they don't, freeze a card in their arsenal or an ally” is unless + at-resolution freeze (Glacial Horns). `payer:"any"` makes unlessEscapeIsAvailable return null and skip the pay chooser — use `payer:"opponent"`. Do not use `hasStatus:arsenal-card` (no handler). Union is Ally typeBox or face-down/face-up-in-arsenal. Start-phase CE expiry must unstamp the frozen marker (`dropExpiredPlayerTurnStartEffects`) or toBeFrozen stays true after the CE is dropped. Polar Blast unless-dominate was already live — UPR147–149 / ELE166 — `packages/engine/src/rules/reducers/assets-turn.ts` + `packages/cards/src/cards/UPR/actions/UPR147-cold-snap-red.test.ts` (2026-08-27)
- [filter] printed “Guardian off-hand with 3 or less base {d}” is `zones:["weapon"]` (Off-Hand seats weapon2) plus `typeBox.types:["Guardian","Off-Hand"]`. Class and Off-Hand live in types[]. Pin that used Warrior Bastion of Unity was a false diagnosis. Fixture `defenseCounterTotal: -1` then `toHaveDefenseCounters(0)` — DYN039–041 — `packages/cards/src/cards/DYN/actions/DYN039-reinforce-steel-red.test.ts` (2026-08-27)
- [restrict] printed “cards defending this can't gain {d}” is restrict `gain-defense` `filter.defending:true` `duration:this-combat-chain` (Snag's gain-power sibling). Smash resolves before Defend so there are no latched defenders — use atom.filter, not subject. apply-atom skips defense-add while the gaining object matches. Combat math is the proof (6{p} vs printed 1{d} → life 15); galvanize UEOT can apply after the chain closes (CR 6.3.5) so do not assert post-combat {d} on Blade Break GY. Mixed family also: Lay Waste restrict-defend Equipment, Tri-Shot `modify-activation-limit` additional 2 (bow `limit.per==="turn"`), Apocalypse play-static evo-count — SUP133 / EVO209 / EVR089 / TCC011 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/SUP/actions/SUP133-smash-with-big-rock-yellow.test.ts` (2026-08-27)
- [restrict] printed “opponents must choose this as the target of attacks if able” is require `be-attacked` `subject:self` (quoteFabAttackTargets). Default `playAttack` targets the hero and throws; prove with explicit `{ target: auraId }` then Spectra destroy (hero life unchanged). Blood-debt skip is restrict `lose-life` gated by `playerBanishedPower6` (Ebon Fold banish a 6{p} card). Blood debt fires on that hero's end phase, not the opponent's. Event “attacks each hero” is 1v1 out-of-scope. Brutus clash-tie choose-winner is not Victor reclash — split `engine/clash-tie-choose-winner` — MON005 / MON119 / SMP028 — `packages/engine/src/rules/legality/attack-targets.ts` + `packages/engine/src/procedures/turn/stages/cleanup.ts` (2026-08-27)
- [sigil] printed “at the beginning of your action phase, destroy this / when this leaves the arena …” is the AUA025/ROS230 cycle (own endTurn, opponent endTurn, then action-phase-start destroy). Pin deck-top proofs with `deckTop` after end-phase draw-to-intellect (4 fillers then the proof card; last of `deckTop` is the top). Leave-arena deal-arcane is at-resolution `any-hero`, never on-stack (the trigger is not a play). Wizard Instant `arcane:1` also pings on play. Play-from-banished grant is `play(card, { from: "banished" })` after `optionals:"accept"` — OMN201 / ROS133 / ROS168 / ROS182 — `packages/cards/src/cards/AUA/actions/AUA025-sigil-of-deadwood-blue.test.ts` (2026-08-27)
- [usurp] printed “When an attack usurps this, it gets +1{p} / overpower / go again” grants onto binding `attack` (finalize already stamps the usurping attack). Observe `source`/`object` so the aura is THIS usurped permanent. Lust create-token is the aura controller — usurp+destroy mints 2. Drive with `play(usurpAttack, { target: auraId })`, never default `playAttack` (that hits the hero). — IAR154 / IAR155 / IAR157 — `packages/engine/src/procedures/play-card/finalize.ts` (2026-08-27)
- [ally] Dragon allies without printed Attack attack via Storm of Sandikai's granted Attack (Dominia path), never a missing CR 8.5 ally-attack primitive. Equipment seats are `equipment-head|chest|arms|legs`, not catalog `"equipment"`. “If 1 or more red cards are revealed this way” is `compare-amount` of `count` `cards-revealed-this-way` `color:red`, never `has-status colored-cards-revealed-this-way`. Nested sequence/if-you-do as the then-leaf drops the add-counter; keep add-counter as the then leaf, bind the countered object as `it`, then conditionally destroy it at evaluated 0{d}. Conditional continuous `rule-modification` effects must declare `duration: "while-condition"`; prove both restriction legs and their expiry. — UPR007 / UPR011 / UPR015 — `packages/cards/src/cards/UPR/allies/UPR015-themai.test.ts` (2026-08-27)
- [combat] printed “While this is defending, if you've been booed/cheered this turn, this gets +1{d}” is PEN271 while-static `and` of `defending` + `been-booed/cheered-this-turn` then `modify-numeric` `self`, never nested `conditional` (CE collector ignores that leaf) or `binding:it`. Charm Instant on the attack layer is `play(charm,{modeIndexes:[1]})` after attacker `pass`, then `advanceUntil({stopAt:"defend"})`. Big Bully “base {p} is doubled” is continuous `multiply` gated by the same stamp. — SUP123 / SUP061 / SUP067 — `packages/cards/src/cards/SUP/blocks/SUP123-disdainful-delight-yellow.test.ts` (2026-08-27)
- [clash] printed “When you win a clash revealing this” observes `source`/`revealed-card` (Rapturous Applause), never `observes:none` (that fires on any clash win). Drive is deck-top THIS + clash-on-defend (Test of Might). Up the Ante is a modal wager AR (`playReaction({modeIndexes})`); hit + `ordering:listed` because Olympia's first-wager Gold is simultaneous. — HVY077 / HVY059 / HVY103 — `packages/cards/src/cards/HVY/actions/HVY077-thunk-red.test.ts` (2026-08-27)
- [latch] printed “whenever it deals damage, attacks gain +1{p} this combat chain” is delayed-trigger `appliesTo.next` `count:all` `duration:this-combat-chain` (not an at-resolution chain scan). Follow-up Snatch inherits every damage instance (fused arcane + hit). — ELE067 / ELE068 / ELE069 — `packages/cards/src/cards/ELE/actions/ELE067-explosive-growth-red.test.ts` (2026-08-27)
- [latch] printed crush “their first action/attack during their next turn” is BVO `target:attack-target` + `until-end-of-their-next-turn` + `appliesTo.next` `ordinal:1`, never `this-attack` / `until-end-of-next-turn`. “If the defending hero controls an aura token, +1{p}” is a while-static `control-object` (resolution has no defending hero yet). Token filter is `metatypes:Token` + `subtypes:Aura`, never `name:"Aura token"`. — WTR061 / WTR068 / MPG034 — `packages/cards/src/cards/WTR/actions/WTR061-cartilage-crush-yellow.test.ts` (2026-08-27)
- [filter] printed “destroy an aura token they control” is `player:attack-target` + Token+Aura typeBox, never `name:"Aura"`. Mixed crush/arsenal-play/ally-damage families must split. — MPG014 — `packages/cards/src/cards/MPG/actions/MPG014-break-stature-yellow.test.ts` (2026-08-27)
- [combat] printed “while defending an AAC with cost 0” is has-status `defending-attack-action-card-with-cost-0`, sibling of `defending-a-weapon-attack` (subject defending + attack is Action+Attack with evaluated cost 0). — EVR050 / EVR051 / EVR052 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/EVR/defense-reactions/EVR050-wax-on-red.test.ts` (2026-08-27)
- [combat] printed “When this defends, if you have less {h} than the attacking hero, the crowd cheers you. While this is defending, if you've been cheered this turn, +3{d}” is defend event-and-state `life-comparison` vs `attacking-hero` plus PEN271 while-static `and` of `defending` + `been-cheered-this-turn`. Nested `conditional` / `binding:it` is inert. passBoth keeps the chain open for the {d} assert; Pleiades Confidence needs `closeCombat({ordering:"listed"})` / `resolveUntilIdle`. Superstar enter-arena cheer is the same stamp. — APS030 / APS024 / SUP012 — `packages/cards/src/cards/APS/blocks/APS030-turning-point-blue.test.ts` (2026-08-27)
- [freeze] printed “destroy a frozen card in their arsenal” reads the frozen marker (Cold Snap already stamps it). Hit unless-pay is `payer:opponent`; destroy is at-resolution `player:attack-target` on arsenal+permanent. Nested optional-destroy after unless needs two booleans — flatten to required destroy after they don't pay. RP 0 still offers unless if they can pitch. — PEN231 / PEN232 / PEN233 — `packages/cards/src/cards/PEN/actions/PEN231-conquer-the-icy-terrain-red.test.ts` (2026-08-27)
- [keyword] printed AR “destroy this: target [filter] attack gets go again” is Snapdragon grant-property onto combat-chain. `untilIdle` after activate closes combat (pin `notToHaveKeyword` was vacuous). Drive `advanceCombatTo("reaction")` + `passBoth` + `toHaveKeyword("go-again")` + AP refund. Aim filter seats `arsenal: [{ card, state: { aimCounters: 1, faceDown: false } }]`. “Your attacks this turn get [whenever they add a hand defender, deal 1]” is grant-property ability + `appliesTo.next` `count:all`; granted trigger observes `source`/`defended-attack` + together-with `playedFromZones:hand` (Unity origin). Never `source`/`defender` (that is Unity on THIS defending). Play() of the non-attack leaves the stack — `resolveUntilIdle` before the follow-up attack. — AAZ007 / ELE225 / DTD198 — `packages/cards/src/cards/RNR/equipments/RNR007-snapdragon-scalers.test.ts` (2026-08-27)
- [leave-arena] Ward aura leave-arena is opponent combat damage (Brutal Assault 4{p}, not Snatch — on-hit draw opens simultaneous-player order). Two Ward auras need `ordering:listed` and will both destroy — pair Flicker with a no-Ward Lightning aura (Sigil of Lightning). Optional banish-then-return-with-holo is Blur Reality's whole sequence inside `optional.effect`, never `optional.then` (that leaves the card banished). Suspense exhaustion is Hungry-for-More endTurn cycles. — AZS017 / OMN005 / SUP176 — `packages/cards/src/cards/OMN/instants/OMN005-flicker-reality-blue.test.ts` (2026-08-27)
- [delay] printed “at the beginning of their next end phase, they discard their hand and destroy their arsenal” after a resolving Action is Stone Rain: resolution `delayed-trigger` windowed `during-their-next-end-phase` `matching:first` actor `opponent`. A source-static on the Action dies in GY. Drive is controller `endTurn` then their `endTurn` then `untilIdle`. End-phase draw-to-intellect follows the beginning trigger — prove the original cards in GY, not hand count 0. Dash Database / start-game items are a different family. — ROS247 — `packages/cards/src/cards/AAZ/actions/AAZ016-stone-rain-red.test.ts` (2026-08-27)
- [amount] 1v1 “at the beginning of each hero's end phase, they lose X{h}, where X is auras they controlled” is two end-phase triggers (`actor: ability-controller` / `opponent`) each counting that player's Aura permanents. Never play-time `{ type: "x" }`. — SEA256 — `packages/cards/src/cards/SEA/actions/SEA256-riddle-with-regret-red.test.ts` (2026-08-27)
- [cost] on-stack target `count: { type: "x" }` reads the play-time chosen X from `costBindings`. Activate already passed `bindings: procedure.costBindings`; play-card declaration context omitted them so `declaredObjectTargetBounds` returned null (“The target count is unsupported”). X=0 auto-binds no targets. Construct shuffle filter is the Construct face name. Guardian off-hand remove-counters is `zones:["weapon"]`. — MST227 / MPG021 — `packages/engine/src/procedures/play-card/declarations.ts` + `packages/cards/src/cards/MST/actions/MST227-supercell-blue.test.ts` (2026-08-27)
- [amp] printed Instant “Discard this: if an action or instant you control would deal arcane this turn, instead plus 1” is Amp 1 (`type: "amp"`), same leaf as Photon Splicing. A `replacement` whose modification is `modify-numeric` power on `self` is not canonical and throws. — ROS170 / ROS171 / OSC022 — `packages/cards/src/cards/ROS/actions/ROS170-chorus-of-the-amphitheater-red.test.ts` (2026-08-27)
- [search] search `to: permanent` already seats Items (Hyper Driver) and auras. Inverted PEN067 pin used the token module in deck. Drive `activate` then `resolveUntilIdle({ entityTargetCanonicalId })`. Prism Herald-to-soul figment search uses the same leaf with `closeCombat` pause (DTD001/DTD002). — PEN067 — `packages/cards/src/cards/PEN/actions/PEN067-assembly-module-blue.test.ts` (2026-08-27)
- [modal] printed “Choose 1 or more” is `modal.choose: { type: "one-or-more" }` (min 1). Empty `modeIndexes:[]` is rejected. Printed “Choose any number” stays `any-number` (empty is legal) — do not re-encode charms onto one-or-more. — PEN225 — `packages/engine/src/abilities.ts` + `packages/cards/src/cards/PEN/actions/PEN225-monolith-of-galcia-blue.test.ts` (2026-08-27)
- [amount] printed “whenever this banishes a card and this has banished another card with the same name” is `compare-amount` of `count` `banished-this-way` `groupBy: "name"` ≥ 2 after the on-hit banish sequence, never `has-status banished-another-card-with-same-name`. Sequential banishes accumulate the this-way cohort (producer + `layerWithEventBindings`). Same-color Bonds of Attraction still uses the turn-ledger handler. — MST115 / MST116 / MST117 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/MST/actions/MST115-bonds-of-memory-red.test.ts` (2026-08-27)
- [trigger] printed “While this is in your graveyard, at the start of your turn …” is `functionalZones:["graveyard"]` on the triggered static. `snapshotFunctionalTriggerSources` already walks GY; `isFunctionalInZone` uses that list (default omits graveyard except complete-contract). Grandfather `has-status in-your-graveyard` as the intervening-if. Two GY copies are simultaneous — `untilIdle({ ordering: "listed", optionals: "accept" })`. — HNT150 / OMN245 / PEN137–139 — `packages/engine/src/rules/snapshots.ts` + `packages/cards/src/cards/HNT/defense-reactions/HNT150-loyalty-beyond-the-grave-red.test.ts` (2026-08-27)
- [cost] printed “destroy X Hyper Drivers / Gold you control” is `isVariableDestroyPlayCost` (`count:{type:"x"}`). Arena tokens are already in `DESTROY_PLAY_COST_ZONES`. Boost on a 1-steam Hyper Driver destroys it before the next play — seat `steamCounters: 2`. “+3{p} for each … destroyed this way” is `repeat` `times` `count` `destroyed-this-way`, not add the count. Answer destroy-cost with `.target` after `playAttack({ xValue: 1, stopAt: "on-attack" })`. Equip X from inventory is a different family. — EVO140 / MST226 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/EVO/actions/EVO140-moonshot-yellow.test.ts` (2026-08-27)
- [keyword] CR 8.5.58 Sharpen removes all +1{p} counters from the sharpened card at the beginning of the end phase. Bundle `numeric-counter-removed` with the end-phase event (Quell sibling) and drop `sharpened-this-turn`. Repeat-of-sharpen putting 0 counters is a different family. — PEN054 — `packages/engine/src/procedures/turn/stages/cleanup.ts` + `packages/cards/src/cards/PEN/actions/PEN054-cut-n-carve-red.test.ts` (2026-08-27)
- [keyword] printed “Sharpen … for each … destroyed this way” is `times` as the TOTAL application count (default 1 when omitted; 0 is a no-op). Do not add a phantom base +1 — that was the additional-time replacement shape (`additionalSharpenExtra` on the event). — MPW066 — `packages/engine/src/rules/proposals/effects/sharpen.ts` + `packages/cards/src/cards/actions/off-beat.test.ts` (2026-08-30)
- [latch] printed “the next attack you boost this combat chain” is `appliesTo.next` `hasStatus: boosted` (rewrites to `wasBoosted` / declaration-fact boost). Duration `this-combat-chain` dies at chain-close — Boost the hitting attack so a Mechanologist deck-top refunds AP and the follow-up stays on the same chain. A later new combat is not this chain (pin `notToHaveKeyword` was a closed-chain follow-up). — CRU106 / CRU107 / CRU108 — `packages/engine/src/rules/continuous/reconciler.ts` + `packages/cards/src/cards/CRU/actions/CRU106-high-speed-impact-red.test.ts` (2026-08-27)
- [amount] printed “for each hero that controls more Gold than you, steal a Gold; if you gain control of 1 or more this way” is 1v1 `compare-amount` opponent Gold `gt` controller Gold then `steal`, wrapped in `if-you-do` for the next-attack rider. Never `has-status controls-more-gold-than-you` / `gained-control-of-1-or-more-gold-tokens-this-way`. — SEA176 / SEA177 / SEA178 — `packages/cards/src/cards/SEA/actions/SEA176-mutiny-on-the-battalion-barque-blue.test.ts` (2026-08-27)
- [combat] printed “when this defends an attack with {p} greater than its base” is `object-numeric-comparison` current gt base on `this-attack` (Thump/Over the Top), never `has-status defended-attack-power-greater-than-base`. Grandfather the slug as a delegate. Rapid Reflex +{p} must `passBoth` before the Trap DR; the defend trigger is a second layer (Drag Down). — OUT173 / HNT214 / OUT106 / MST190 — `packages/engine/src/rules/evaluation/conditions/object-numeric-comparison.ts` + `packages/cards/src/cards/OUT/defense-reactions/OUT173-inertia-trap-red.test.ts` (2026-08-27)
- [combat] printed “While this is in your arsenal, if you've controlled a Toughness this turn, this gets ambush” is a while-static `and` of `in-your-arsenal` (zone === arsenal, faceInArsenal sibling) plus existing `controlled-toughness-this-turn`, grant-property ambush. Nested `conditional` on a while-static is inert. Strip unprinted `keywords:[ambush]`. Prove arsenal `defendWith` vs hand gate. — PEN294 / PEN295 / PEN296 / SUP020 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/PEN/actions/PEN294-stadium-security-red.test.ts` (2026-08-27)
- [boost] printed “when you boost, remove a steam counter from this and gain {r} / the attack gets +2{p}” is live on Hyper Driver / Teklo Pounder. Seat Teklovossen; `attackWith({ boost: true })` then steam-2 and RP/+{p}. Stale pin was a missing repeated-boost drive. — ARC036 / EVR072 — `packages/cards/src/cards/ARC/actions/ARC036-hyper-driver-red.test.ts` (2026-08-27)
- [amp] printed “while this is face-up in any zone, if it would gain {p}, instead plus 1” is Back Heel Kick `rule-modification` `mode:amplify` `action:gain-power` `subject:self`. `type:replacement` `replaces:gain` never compiles. Face-up-in-any-zone while-statics are already identity-anywhere. Nimblism Red + face-up Arrow 2+3+1=6; Might + Doubling Season 0+1+1=2 (pitch after EOT RP reset). — OUT100 / PEN218 / BEN008 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/OUT/actions/OUT100-amplifying-arrow-yellow.test.ts` (2026-08-27)
- [crush] printed “they lose all hero card abilities until the end of their next turn” is already `restrict` `lose-abilities` on `attack-target` (Humble / BVO018). Enigma chi-create is a bad restore probe; Bravo `expectActivationRejected` is the strip. — WTR063 / WTR064 / BVO018 — `packages/engine/src/rules/bvo-gap-paths.test.ts` + `packages/cards/src/cards/WTR/actions/WTR063-crush-confidence-red.test.ts` (2026-08-27)
- [restrict] printed “can't play or activate more than 1 action during their next turn” is two `restrict` leaves (play + activate) of Action, `limit.count: 1`, `subject: attack-target`, `duration: until-end-of-their-next-turn`. Honor `limit` in play/activate legality from this-turn Action play + activation history (shared cap; first Action Instant-activation is legal, second Action play is not). Instant is not Action. Split from mixed on-hit-rider. Guardian-aura defend-ban + hit-destroy is Lay Waste / ROS216 (`player: attack-target`). — ARC043 / SUP130 — `packages/engine/src/rules/legality/restrict-cap.ts` + `packages/cards/src/cards/ARC/actions/ARC043-red-in-the-ledger-red.test.ts` (2026-08-27)
- [optional] printed “You may … target dagger” is at-resolution inside `optional`, never on-stack. On-stack declared targets ARE the may (CR 1.8.5e auto-accept). Triggered at-resolution optionals already open (APS028). — HNT197 / APS028 — `packages/engine/src/rules/layer-resolution/decisions/optional.ts` + `packages/cards/src/cards/HNT/attack-reactions/HNT197-perforate-yellow.test.ts` (2026-08-27)
- [trigger] GY start-phase “you may destroy 2 Silver; if you do, return this” is `functionalZones:["graveyard"]` (HNT150). Seat the GY card on P2 so P1 `endTurn` is their start; drain `optionals:"accept"` `entityTargets:"maximum"`. Surge +X{d} while defending is live `count` Seismic Surge. — PEN142 / MPG023 — `packages/engine/src/rules/snapshots.ts` + `packages/cards/src/cards/PEN/instants/PEN142-seeker-kunai-red.test.ts` (2026-08-27)
- [amount] printed “If a Golden Cog is destroyed this way, create a Gold” is `compare-amount` of `count` `destroyed-this-way` `name: Golden Cog` inside galvanize `optional.then` (Adaptive Plating). Never `has-status destroyed-this-way-golden-cog` (a sibling of the optional fail-louds on decline). SEA “and repeat this process” is `repeat until:declined` of that optional; a no-else then-branch stops after a non-Cog. Drive the extra iteration with `accept()`/`.target` then `decline()` (Gold is an Item). — PEN165 / SEA004 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/PEN/actions/PEN165-skywarden-no-161803-yellow.test.ts` (2026-08-27)
- [cost] printed “{t} X Seismic Surge you control. Destroy X target aura tokens” is `isVariableTapPlayCost` (destroy-X sibling: play-x, play-cost-target, `set-tapped`). Resolution destroy X reads `costBindings.x`. Aura tokens are Token+Aura; 1v1 is `player: any`. Unique Surge auto-binds; `playInstance(..., "explicit")` then `.target` the opposing aura. — PEN020 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/PEN/actions/PEN020-seismic-shift-red.test.ts` (2026-08-27)
- [sharpen] the end-phase `numeric-counter-removed` events leave the `sharpened-this-turn` marker; the marker dies in the advance-turn reducer sweep (assets-turn.ts) so next-turn conditionals (Zenith Blade a2 go again) re-arm correctly, and the cleanup scan skips `amount < 1` (the reducer fail-closes count 0). — PEN054 / AHA011 — `packages/engine/src/rules/reducers/assets-turn.ts` + `packages/engine/src/procedures/turn/stages/cleanup.ts` (2026-08-27)
- [combat] "When this chain link resolves, if there is a card defending this" is `has-status card-defending-this` = active-attack identity + `combat.defending.length > 0`, evaluated while the link is still open; holds on blocked links regardless of damage (0{p} attacks included) — PEN036 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` (2026-08-27)
- [history] "if you've created a Crouching Tiger this turn" follows the six-site producer template: HistoryTokenKind union + slug table (history-facts.ts), turn-history field + default, create-case stamp (transaction-kernel `historyTokenKindOf` switch), state-rules-view facts map, rules-view field, has-status handler. Play note: DRs are PLAYED at the reaction step (`must.playReaction`), never blockWith; mid-combat drains use passBoth — `untilIdle` tries to close the combat. — MST163 — `packages/engine/src/kernel/history-facts.ts` + `packages/cards/src/cards/MST/defense-reactions/MST163-territorial-domain-blue.test.ts` (2026-08-27)
- [play] printed “You may play Lumina Ascension as though it were an instant” on an arena Item is static while-in-arena `play-card` `appliesTo.next` `name: Lumina Ascension` `count:all` `asType:instant` (Vengeful Apparition / Gravy Bones). Lumina is MON034 — stale “not authored” pin. No printed “this turn”; a resolution hand-scan without `declared` throws “play permission source is unresolved”. Seat Boltyn; 0 AP after Spirit; opponent-turn Instant after Bravo `pass()`. Unplayable regex `/action-?point cost cannot be paid/`. — DYN066 — `packages/cards/src/cards/DYN/actions/DYN066-spirit-of-eirina-yellow.test.ts` (2026-08-27)
- [trigger] printed “Whenever [this] deals or is dealt damage” is two `dealt-damage` source observations (`damage-source` / `damage-target`), never `observes:none` (that fires on any damage). `kind:source` + `damage-target` needs the trigger spec’s source allowlist (cards.ts + types). Dragon ally attacks via Storm of Sandikai; targeting the ally uses `attackWith({ target: allyId })`. Snatch-on-hit vs ally is simultaneous-player — use a no-on-hit AAC (Wounding Blow). — UPR013 — `packages/engine/src/cards.ts` + `packages/cards/src/cards/UPR/allies/UPR013-nekria.test.ts` (2026-08-27)
- [history] printed “Attack with target sword that has been sharpened and you've attacked with this turn” is on-stack `attack-with` whose filter is Sword + `hasStatus:sharpened-this-turn` + `hasStatus:attacked-with-this`. Never wrapping `attacked-with-target-this-turn`. Combat attack stamps `weaponAttackInstanceIdsThisTurn`; the FILTER handler matches THIS instance (Hell Hammer’s condition reads the same list). Unique sword still needs `advanceUntil({ stopAt: "defend" })` after play. Unused RP resets EOT — pitch a leftover card to reach the no-target quote. — MPW028 — `packages/engine/src/rules/reducers/combat.ts` + `packages/cards/src/cards/MPW/actions/MPW028-and-again-blue.test.ts` (2026-08-27)
- [harness] printed “if an ally has been put into your graveyard this turn” is zone-count `per:turn` on the controller GY (already live). Do not attack an opposing ally (wrong GY). `play(attack,{target:allyId})` then a later hero attack; Temper at 2{d} keeps the seat. Fixture GY is not this turn. — PEN153 — `packages/cards/src/cards/PEN/equipments/PEN153-mournful-casket.test.ts` (2026-08-27)
- [clash] printed “If all heroes in a clash would fail to win, instead choose which hero wins” is outcome replacement of `clash-outcome` + `win-clash` when `winnerId` is null (1v1: controller wins the tie). Not Victor reclash. Clash prizes must still be staged on a tie so the deferred prize sees the assigned winner. — JDG024 — `packages/engine/src/kernel/replacements/admission.ts` + `packages/engine/src/rules/proposals/effects/clash.ts` + `packages/cards/src/cards/JDG/heroes/JDG024-brutus-summa-rudis.test.ts` (2026-08-27)
- [play] printed crush “they can't play face-up from arsenal, and you may play face-up cards from their arsenal until the end of your next turn” is restrict-play `subject:attack-target` plus `play-card` from `player:attack-target` arsenal `hasStatus:face-up` `count:all`, duration `until-end-of-own-next-turn`. Play-permission duration must include that duration. begin-play locates cross-owner arsenal; quote waives the own-zone lock only when a play-card permission names the object (`playInstance(..., { from: "arsenal" })`). Stolen card GY is the acting player. — MPG029 — `packages/engine/src/rules/legality/play.ts` + `packages/engine/src/commands/handlers/begin-play.ts` + `packages/cards/src/cards/MPG/actions/MPG029-annexation-of-all-things-known-yellow.test.ts` (2026-08-27)
- [play] Dash Database “Mechanologist item cost 0 or 1 from the top of your deck as though it were an Instant; additional {r}” is already static play-permission `fromZones:["deck"]` `position:"top"` `asType:instant` `costModification.increase:1`. Public verb is `play(card, { from: "deck" })`. Look-at-any-time is viewer-facing. Extra {r} is `/resource cost cannot be paid/`; a non-item top is `/migrated permission/`. — EVO002 — `packages/cards/src/cards/EVO/heroes/EVO002-dash-database.test.ts` (2026-08-27)
- [replacement] a NEW canonical replacement shape needs THREE sites, not one: a recognizer in admission.ts (supportedCanonicalReplacement), a candidate-collection branch in collect.ts (per-shape matching — for draw events `replaces.filter` qualifies event.source, the Gold token), and an apply branch in apply.ts (cancel original via event:null + queue subEvents). Missing collector/apply = registered-but-never-applied passthrough. Player-redirect draws draw from the redirect TARGET's own deck top. — SEA149 — `packages/engine/src/kernel/replacements/{admission,collect,apply}.ts` (2026-08-27)
- [cost] "As an additional cost to play this, banish your hand" is `count: {type:"all"}` hand-banish (DTD111): FOUR wiring sites — isPayablePlayCost gate (legality/play.ts reads it via effect-cost recognizers; missing = "requires an unmigrated effect-cost declaration"), declarations.ts auto-bind branch (whole filtered hand, no selection decision, empty hand vacuously payable), payment.ts dispatch cohort (stamps banished-this-way/them/it + 6{p} counts), and the recognizer. — DTD111 — `packages/engine/src/procedures/play-card/{effect-costs,declarations,payment}.ts` (2026-08-27)
- [equip] printed “Equip X weapons and/or equipment from your inventory” is live `type:equip` from inventory (Fabricate). `typeBox.types:["Weapon","Equipment"]` is AND — only Weapon-Equipment dual types match; printed and/or is `or` of two type-boxes. At-resolution `{ type: "x" }` must `resolveLayerAmount` from play `costBindings` before the pool scan (objectTargets already did; find-decision did not — scan requires a number). Exact X=0 persists empty so a non-empty pool does not open a chooser / unresolved-target. Seat Olympia. Unique Gold auto-binds the destroy-X cost. — MST226 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/MST/actions/MST226-visit-the-golden-anvil-blue.test.ts` (2026-08-27)
- [repeat] printed “For each non-attack action revealed this way, put an AAC revealed this way into your hand” is `repeat` `times` `count` `revealed-this-way` filtered to Action exclude Attack, then move-card 1 AAC from that cohort. Hero `for-each` cannot iterate a revealed object binding (`it` is not a hero set). Damage is `put-into-hand-this-way` (sibling of put-into-graveyard / put-on-bottom), never `drawn-this-way`. Merge reveal + follow-up into one sequence so the this-way stamps carry. Cost X omits numeric `cost`. — MON231 — `packages/engine/src/rules/proposals/card-movement-effects.ts` + `packages/cards/src/cards/MON/actions/MON231-sonata-arcanix-red.test.ts` (2026-08-27)
- [replacement] printed “if this would be put into your graveyard from anywhere, instead banish it” is already `isGraveyardToBanishReplacement` (destroy/move-zone to GY → banish). d2 Temper does not die on the first defend — current {d} must be ≤1 (second defend, or a prior −1{d}). Blood Debt taxes at the owner's end phase (P1 `endTurn` then P2 `endTurn`). — PEN192 — `packages/cards/src/cards/PEN/equipments/PEN192-embraforged-gauntlet.test.ts` (2026-08-27)
- [material] Galvanic Bender is the same Dust Material while-static (`source-is-subcard-of-host` + host +1{p}). Prove it through the legal Construct Nitro Mechanoid declaration and transform flow; the resulting host is 6{p}. Do not use fixture-only `hostUnder` as behavioral proof. — DYN089 — `packages/engine/src/rules/invocation-construct-transform.test.ts` (2026-08-27)
- [cost] printed “put a card from your arsenal on the bottom of your deck” is `isMoveToDeckPlayCost` `from:"arsenal"` (hand was already first-class). Quote used to list unmigrated `effect-cost` because the recognizer only admitted hand. Scan arsenal, auto-bind the unique card (CR 1.8.6c), reverse when empty. — ELE118 / ROS035 — `packages/engine/src/procedures/play-card/effect-costs.ts` + `packages/cards/src/cards/ELE/actions/ELE118-tome-of-harvests-blue.test.ts` (2026-08-27)
- [evo] Instant Evo “transform it into this, then equip this” is Zoom Call: transform the equipped Base+seat, not `self`. “When this is equipped, up to 1 target attack gets go again” is at-resolution `typeBox.types:["Attack"]` (Attack lives in types[]). Drive `toReaction("attacker")` then `untilIdle({ entityTargets: "pause" })` then `.target(attack)`. Empty `.target()` is the decline. — EVO053 — `packages/cards/src/cards/EVO/equipments/EVO053-evo-zip-line-yellow.test.ts` (2026-08-27)
- [restrict] printed “its {r} cost / {p} / {d} can't be modified” is restrict `be-modified` `subject:self`. Drop `has-status in-any-zone`. A self-targeted while-static is functional wherever the source is (CR 5.4.7). apply-atom skips power/defense/cost numeric atoms. — DTD201 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/DTD/actions/DTD201-numbskull-red.test.ts` (2026-08-27)
- [history] printed “each opposing hero that has dealt damage to you this turn loses 1{h}” reads the victim's per-source damage-taken map. Stamp `damageTakenBySource[sourceInstanceId]` at the dealt-damage victim block and attribute non-ally damage to the dealer's hero (CR 8.2.8e); hero targets map to player life events and living objects retain object targets. Empty scans are legal no-ops. After combat use `passPriorityTo` before the Instant. — HNT016 — `packages/engine/src/rules/reducers/combat.ts` + `packages/engine/src/rules/proposals/asset-effects.ts` + `packages/cards/src/cards/HNT/instants/HNT016-anaphylactic-shock-blue.test.ts` (2026-08-27)
- [amp] printed “Amp X, where X is the total {h} you've gained this turn” is `type:amp` `count` `life-gained-this-turn` (Chorus Amp leaf). Amp 0 is a determined no-op (return the gain-assets events, skip the replacement), not “amp amount is unresolved”. Meld is `playMethod:{kind:"meld"}` right-then-left with `passBoth` between faces. Seat Verdance. Voltic Bolt 5+X. — ROS017 — `packages/engine/src/rules/proposals/asset-effects.ts` + `packages/cards/src/cards/ROS/instants/ROS017-rampant-growth-life-yellow.test.ts` (2026-08-27)
- [counter] printed “move any number of +1{p} counters from auras you control onto this” is an unqualified `move-counter` leaf that sweeps all matching counters from one other aura. `hasStatus: other-than-source` is a filter operator, and the target-pool count must apply it before exact-N resolution. Empty pools are legal no-ops; auras without base {p} use `toHaveCounters`, not `toHavePower`. — MST133 — `packages/engine/src/rules/proposals/effects/move-counter.ts` + `packages/cards/src/cards/MST/instants/MST133-restless-coalescence-yellow.test.ts` (2026-08-27)
- [counter] printed “Remove a +1{p} counter from target attacking weapon. If you do, …” is live numeric `remove-counters` + `if-you-do` (empty events when the object lacks the stack). Filter `hasStatus: attacking` + Weapon type-box; unique weapon auto-binds. `activateAttack` then `toReaction("attacker")` then `play` then `passBoth` keeps the chain open for 4→3{p}. An attacking AAC is not a legal target. Split leftover from Guardian off-hand −1{d}. — AHA021 — `packages/engine/src/rules/proposals/counter-status-effects.ts` + `packages/cards/src/cards/AHA/attack-reactions/AHA021-ol-blue.test.ts` (2026-08-27)
- [restrict] printed crush “the only attacks they may play or activate … are attacks with base {p} greater than the damage dealt this way” is complementary restrict play+activate of Attack/Weapon `numeric` base {p} `lte` `event-amount`, `subject: attack-target`, `until-end-of-their-next-turn`. Restrict of the allow-list inverts. Lock the event-amount onto the CE filter at generation (modify-numeric sibling; next-turn quote has no layer). Seat Bravo for specialization. — DTD203 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/DTD/actions/DTD203-star-struck-yellow.test.ts` (2026-08-27)
- [roll] printed “if you would roll 1 or more dice this turn, instead roll that many plus 1 and ignore the lowest” is a this-turn roll replacement (`extraDice:1` `ignore:"lowest"`), never `set-status roll-plus-one-ignore-lowest`. Admission + apply stamp extraDice on roll-request; reducer commits all faces and keeps max. `lastDieFaces()` is the extra-die proof; `lastDieFace()` is the kept result. Seat Kayo. — EVR003 — `packages/engine/src/kernel/replacements/{admission,apply,collect}.ts` + `packages/cards/src/cards/EVR/actions/EVR003-ready-to-roll-blue.test.ts` (2026-08-27)
- [amount] printed “If 3 or more Runechants are created this way” is `compare-amount` of `count` `created-this-way` ≥ 3, never `has-status 3-or-more-created-this-way-runechants`. Create-token already stamps the number. Strip unprinted `keywords:[goAgain]`. Viserai’s play-Runeblade extra Runechant is not this-way — 2 this-way + 1 hero mint is still no go again. — HNT254 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/HNT/actions/HNT254-douse-in-runeblood-red.test.ts` (2026-08-27)
- [history] printed “If you've activated a weapon this turn” is `performed-this-turn` `activate-weapon` (cannon sibling). Stamp on activate when the source type-box has Weapon. Never `has-status activated-a-weapon-this-turn`. Instant prevent vs a 2-arcane Instant (Flash Bolt Yellow) distinguishes prevent-1 vs prevent-2. Defender Instant window is `toReaction("defender")`; no-combat Instant is `passPriorityTo`. — PEN043 — `packages/engine/src/kernel/transaction-kernel.ts` + `packages/cards/src/cards/PEN/equipments/PEN043-templar-spellbane.test.ts` (2026-08-27)
- [guess] printed “If they guessed wrong, they discard” nests look/choose-color/guess/discard inside `optional`. The guess proposal stamps `guessed-color`; grandfathered `guessed-wrong` compares it with looked binding `it`, while discard scans the attacking hero's hand. Drive `untilIdle({ optionals:"accept", entityTargets:"pause" })`, then defender `.choose(color)` and attacker `.choose(color)`. — SUP077 — `packages/engine/src/rules/evaluation/conditions/has-status.ts` + `packages/cards/src/cards/SUP/blocks/SUP077-truth-or-trickery-yellow.test.ts` (2026-08-27)
- [scrap] printed “Gain {r}{r} for each card this scrapped” is live `gain-resources` of `sum` of two `cards-scrapped-by-this` counts. `{ scrap: true, scrapCard }` pays one Scrap; RP lands at resolution (`passBoth` under FAB_MANUAL_HARNESS — on-stack RP is still 0). Seat Teklovossen. — EVO101 — `packages/cards/src/cards/EVO/actions/EVO101-scrap-trader-red.test.ts` (2026-08-27)
- [modal] "If you choose both/all modes" riders: finalize stamps the flat play binding `chose-both` when declaredModes.length covers every printed mode (>=2); has-status `chose-both` reads that binding from the resolving layer. Modal mode choices enforce the printed minimum at play time ("Choose 1 or both" = min 1 — no zero-mode leg). The companion "search ... put it on top" leaf already relocates the found card; a trailing move-card self only buries the source card on top of the deck. — PEN263 — `packages/engine/src/procedures/play-card/finalize.ts` (2026-08-27)
- [search] printed “Whenever a Herald is put into Prism's soul during an action phase, you may search a figment into the arena” is live search-to-permanent. Filter `subtypes:["Figment"]` unions types[]. Drive `closeCombat({ optionals:"accept", entityTargets:"pause" })` then `.targetRequired(cardIn("deck", figment))` — min=0 does not auto-bind. — DTD001 / DTD002 — `packages/cards/src/cards/DTD/heroes/DTD002-prism-advent-of-thrones.test.ts` (2026-08-27)
- [cost] printed “you may {t} your hero and destroy this. If you do, …” is mixed `pay` of `tap-hero` + `destroy-self` (optional.then create). proposePay walks each mixed part; unpayable tap-hero (already tapped / missing hero) fails closed so then does not fire. Sibling of Magmatic Carapace resources+tap-self. — AOL004 / AOL005 — `packages/engine/src/rules/proposals/asset-effects.ts` + `packages/cards/src/cards/equipment/prizeworn-gauntlet.test.ts` (2026-08-30)
- [latch] printed hit “your next blue attack this turn gets +1{p} and go again” is `onHit` `plusPower`+`grantKeyword` `appliesTo.next` `color:["blue"]` Attack subtype; omit dummy `this-attack`. Snatch red is printed 4{p} — “got 4{p}” is the color-correct miss, not an over-match (5{p} would be the color-blind latch). — LGS290 — `packages/cards/src/cards/weapons/beckoning-mistblade.test.ts` (2026-08-30)
- [bond] printed “If an Ice/Earth card was pitched to play this, this gets …” is Log Fall resolution `binding-numeric` `pitched-this-way-ice/earth-card`, never a while-static `pitch-zone-has` + `duration: while-condition` (keyword grants on the chain-attack self drop from that sequence). Frostbites “in each exposed head/chest/arms/legs” stay `amongExposed`; assert equipment seats, not arena `toHaveTokenCount`. Dominate proof includes `expectBlockRejected` `errorCode: dominate`. Seed `resourcePoints: 0` so the Ice pitch pays. — IAR260 — `packages/cards/src/cards/actions/ice-aged-oak.test.ts` (2026-08-30)
- [token] printed “Equip a Graphene Chelicera token” is `create-token` `graphene-chelicera` (Whittle), never `equip` `name: "A Graphene Chelicera Token"`. “Next attack with stealth” is `plusPower` `appliesTo.next` `hasKeyword: stealth` (do not require Attack subtype — weapon daggers are stealth attacks). Agent cost discount is `duration: while-in-arena`, never `while-condition` with no condition. — HNT005 — `packages/cards/src/cards/demi-heroes/arakni-orb-weaver.test.ts` (2026-08-30)
- [latch] printed “your next zombie attack” is `appliesTo.next` `typeBox.subtypes:["Zombie"]` (Zombie lives in types[] on Ally/Action; matches-filter unions the type-box. Do not AND Attack — Vox-granted Restless Magister attacks are the real zombie attacks). Empty `reorder-deck` is a determined no-op (skip find-decision pending; proposal empty events) or X=0 stack-overflows. — AMA011 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/actions/dig-for-souls.test.ts` (2026-08-30)
- [clash] printed “If you would fail to win a clash revealing this, instead you win” is clash-outcome replacement `subject:self` (the revealed deck-top), never `replaces.clash-lose` (that event is observation-only). Collection matches `clash-outcome.revealed`; apply rewrites `winnerId` and may queue `crowd-boos`. Prove with this as the clash reveal, not as the attacking card. — GEM094 / SUP093 — `packages/engine/src/kernel/replacements/{admission,collect,apply}.ts` + `packages/cards/src/cards/actions/overturn-the-results.test.ts` (2026-08-30)
- [clash] printed “instead you may destroy a Gold … then clash again” is clash-outcome optional reclash. Stamp `prize` onto clash-outcome as `deferredEffect`, copy it onto reclash-request, and restage one clash-prize after the fresh outcome. Two clash-prize events in the same batch (original + restaged) must enqueue **one** pending layer per clashId — duplicate `pendingTriggerId` makes trigger-order unanswerable (`Order every entry exactly once`) and the winner never gets the token. Unique Gold auto-binds; pause at the reveal chooser. — HVY048 — `packages/engine/src/kernel/transaction.ts` + `packages/cards/src/cards/heroes/victor-goldmane.test.ts` (2026-08-30)
- [harness] printed Instant staff “activate only if you've played a non-attack action this turn / if it's not your turn, instead 3” is Iyslander arsenal blue NA Action as Instant, then `passPriorityTo`, then activate. Instant-type cards are not action cards. `printedCardRole` keeps Action as role `action` when `asType:instant`. — LGS109 — `packages/engine/src/procedures/play-card/finalize.ts` + `packages/cards/src/cards/weapons/waning-moon.test.ts` (2026-08-30)
- [activate] printed “can only attack if you've attacked with a weapon this turn” is an activated Attack `condition` `compare-amount` `weapon-attacks-this-turn` ≥ 1 (Waning Moon shape). A while-static restrict gated by that same ≥ 1 inverts the clause and never sees quote-time facts. Count is already stamped on weapon attack. — FAB191 — `packages/cards/src/cards/tokens/cintari-sellsword.test.ts` (2026-08-30)
- [target] printed “Zombies/allies you control get +N{p}” is `zones:["permanent"]` plus Zombie type-box. Attacking allies leave the arena for the combat chain — `objectMatchesTargetZones` must treat an Ally on the chain (or the active attack) as permanent, the CR 7.2.2b sibling of seated weapons. Do not list `combat-chain` on every you-control aura. Prove with `activateAttack` then `toHaveAttackPower`. — AMA014 — `packages/engine/src/rules/evaluation/targets/object.ts` + `packages/cards/src/cards/actions/restless-commander.test.ts` (2026-08-30)
- [restrict] printed “the attack can't gain {p} this turn” is live restrict-gain-power. CR 6.3.1 applies that rule-mod before object CEs on every recalc (6.3.5), so a prior Nimblism/Pummel +N does not persist. Do not timestamp-filter “future additions only” — Snag vs already-resolved Pummel is the owner. Rapid Reflex after the trap is the future-add proof. — OUT102 / CRU182 — `packages/engine/src/rules/evaluation/power-gain-restrictions.ts` + `packages/cards/src/cards/defense-reactions/buzzsaw-trap.test.ts` (2026-08-30)
- [latch] printed “activate your second sword attack this turn without paying {r}” is `modify-activation-cost` `appliesTo.next` Sword `events:["activate"]` `ordinal:2`. Weapon attack activations have no proxy yet; `futureObservationSubject` must return the physical weapon when the latch watches `activate`, not wait for the later combat `attack` event (that event is ignored because the family is activate-only, so ordinal-2 never counted the first swing). Quote already uses `observedSubjects.length + 1 >= ordinal`. Prove remaining-RP deltas. — MPW018 — `packages/engine/src/rules/continuous/reconciler.ts` + `packages/cards/src/cards/equipment/heart-of-bladehold.test.ts` (2026-08-30)
- [harness] printed “name a card” is public `effect-resolution` “Name a card.” answered by `.choose(printedName)` (seated catalog names: “Snatch”, “Hunter Or Hunted”; default `deck:N` fillers are `fab-test-filler`). After a contract-labeled DR, decline Arakni's look then `passBoth` the stacked defend trigger before the chooser. “cards with the chosen name” keeps the while-static `contract-task` (snapshot filter for `contract-progress`) **and** a resolution `contract-task` after `name-card` to seat `activeContract` (CE collector ignores the while-static discrete leaf). `expectFabCard` does not search deck — assert `zone("deck")`. — SUP245 — `packages/engine/src/rules/layer-resolution/decisions/name-card.ts` + `packages/cards/src/cards/defense-reactions/hunter-or-hunted.test.ts` (2026-08-30)
- [latch] printed “first attack with contract each turn gets go again. If it's attacking a Royal hero, +1{p}” is TWO while-statics, each with top-level `appliesTo.next` (go-again vs `hasStatus: attacking-a-royal-hero`). `extractStaticFutureApplicability` only reads `ability.effect.appliesTo` — a sequence of mixed latches shares one (or no) future and the nested +1 hits current `this-attack` for every hero. Do not nest `binding-matches` on unbound `it`. Royal talent often lives in types[] (Fang); filter+condition handlers check types and supertypes. Include the slug in `latchFilterRequiresDeclaredHeroTarget`. — ARK007 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/mentors/the-hand-that-pulls-the-strings.test.ts` (2026-08-30)
- [amount] printed “the first time each turn another hero becomes the target of a source that would deal lethal damage, you may discard a red card. If you do, choose new targets for that source” is event-and-state `attack` + `compare-amount` `pending-damage-to-hero` vs `hero-property` life (Amulet), never `hasStatus: lethal-target-another-hero` or `set-status choose-new-targets`. Then-leaf is CR 1.8.5f `choose-new-targets` (retarget the live attack to original legal targets; unique auto-binds; empty is a no-op). Limit 1/turn. Unique ally as the only other legal attack-target is the 1v1 retarget proof; a second lethal the same turn is not offered. — JDG001 — `packages/engine/src/rules/proposals/effects/choose-new-targets.ts` + `packages/cards/src/cards/heroes/taipanis-dracai-of-judgement.test.ts` (2026-08-30)
- [out-of-scope] printed “If it's graded Gem Mint 10, put it into your hand” is already `binding-matches` `typeBox.traits:["Gem Mint 10"]`. No authored card carries that trait; do not invent a graded card. The else-leg (opponent may draw) is the 1v1 proof. PSA-only deckbuilding is meta-static. — LSS001 — `packages/cards/src/cards/heroes/ruu-di-gem-keeper.test.ts` (2026-08-30)
- [trigger] printed “Whenever you transcend, you may gain {r}” is a live `transcend` observation (CR 8.5.48). Homage to Ancestors (ENG026) is an authored transcend source — play another blue then Homage, `untilIdle({ optionals: "accept" })`, assert `{r}`. A non-transcend Snatch is the boundary. Do not pin “no authored transcend source”. — FAB227 — `packages/cards/src/cards/equipment/twelve-petal-k-ya.test.ts` (2026-08-30)
- [sequence] printed “Sharpen. If it has 3 or more +1{p}, you may …” needs the sequence prefix rehearsal to stamp sharpen `outputBinding` and simulate the +1{p} on a clone (same path as add-counter). Without that, the nested optional is never asked and later proposal throws `optional choice is unresolved`. — MPW031 — `packages/engine/src/procedures/layer-resolution/find-decision.ts` + `packages/cards/src/cards/actions/honed-for-honor.test.ts` (2026-08-30)
- [optional] printed “You may destroy a Gold … If you do, equip …” already no-ops on decline (`proposeOptional`). Seated Gold modules are not `token:gold` — occupancy is `expectFabCard` / `zone("arena")`, never `toHaveTokenCount`. Vigor/courage/go again are later sequence steps. Do not flatten to required-destroy. — AOL028 — `packages/engine/src/rules/proposals/effects/optional.ts` + `packages/cards/src/cards/actions/visit-the-prize-room.test.ts` (2026-08-30)
- [play] printed AR “Banish the top. You may play it this combat chain” is the Iris duration grant (`play-card` `fromZones:banished` `duration:this-combat-chain` on banish `it`), never `optional` wrapping play-card. `untilIdle` closes combat and expires the grant — `passBoth` and play while the chain is open. — EVR053 — `packages/engine/src/rules/proposals/continuous-rule-effects.ts` + `packages/cards/src/cards/equipment/helm-of-sharp-eye.test.ts` (2026-08-30)
- [binding] printed “reveal a Tome from your inventory: Put it into your hand” is a reveal activation cost that must stamp `revealed-this-way` onto `paidCostBindings` (activate event copies those onto the layer). Move-card then targets that binding, never an inventory rescan + `inObjectBinding`. — JDG062 — `packages/engine/src/procedures/activate-ability/stages/payment.ts` + `packages/cards/src/cards/heroes/the-librarian-magister-of-history.test.ts` (2026-08-30)
- [latch] printed “your second sword attack each turn costs {r} less” is Heart `modify-activation-cost` `events:["activate"]` `ordinal:2` `perTurn:true`, never `modify-numeric` cost on `this-attack` (no proxy at quote). Copper “if you’ve attacked 2+ with weapons, create one per hit” is live `weapon-attacks-this-turn` / `weapon-attacks-that-hit-this-turn`; `endTurn` then `untilIdle` so the end-phase trigger resolves. — CRU077 / 1HP140 — `packages/cards/src/cards/heroes/kassai-cintari-sellsword.test.ts` (2026-08-30)
- [amount] printed “choose 1 … for each card you’ve banished from your soul this combat chain” is triggered-modal `choose` as `count` `cards-banished-from-soul-this-combat-chain`. Stamp on soul→banish (own soul) onto `combatChain`; reset with boostsThisCombatChain. Choose 0 is determined empty modes, not “unsupported amount”. — SUP266 — `packages/engine/src/rules/evaluation/amounts/count.ts` + `packages/cards/src/cards/actions/battlefield-beacon.test.ts` (2026-08-30)
- [play] printed “play Evos from banished” plus “next Evo this turn as though it were an instant” are independent permissions. Origin-authorizing grants name `fromZones`; timing/cost modifiers omit `fromZones` (null) and never open a new origin (CR 5.1.1a). Quote selects the method (CR 5.1.3d) but origin must still be independently authorized — do not OR `asType` onto every selected origin (Astral Etchings still has normally vs instant). Default omitted permission prefers an applicable as-instant grant. — EVO008 / EVO007 — `packages/engine/src/rules/legality/play.ts` + `packages/cards/src/cards/heroes/teklovossen.test.ts` (2026-08-30)
- [equip] printed “You may equip 2H swords as though they were 1H” is catalog `rule-modification` `allow` `equip` `handedness:"2h-sword-as-1h"` (CR 8.2.2 occupancy, not a type change). Pregame and runtime `nextFreeWeaponSlot` already read it; fixture seating must use the same `validateWeaponArea` gate so illegal 2H+1H seats fail. Do not add a cheat `equip()` harness verb. — SPW003 — `packages/engine/src/testing/test-fixtures.ts` + `packages/cards/src/cards/heroes/zane-broadly-beloved.test.ts` (2026-08-30)
- [filter] printed “target attack that has wagered” reads object marker `{kind:"wagered"}` (wager reducer already stamps it). Filter `hasStatus:"wagered"`, never `attack-has-wagered`. — MPW019 — `packages/engine/src/rules/evaluation/matches-filter.ts` + `packages/cards/src/cards/equipment/dealer-s-grip.test.ts` (2026-08-30)
- [restrict] printed “You can't play or activate cards you own” is game-scope restrict `filter.hasStatus: owned-by-controller` (owner === rule controller), plus `other-than-source` so the Bait can still attack. `subject:{hasStatus}` compiles to subjectFilter and is ignored by play/activate quotes. Activate quote must apply game-scope rules and run matchesFilter for non-typeBox fields. Delayed “destroy this when the chain link resolves: Attack” is Razor Ring delayed-trigger, never a delayed destroy-self cost. — FAB393 — `packages/engine/src/procedures/activate-ability/stages/quote.ts` + `packages/cards/src/cards/tokens/bait.test.ts` (2026-08-30)
- [harness] printed look with no follow-up is `lastLookedCanonicalId()` (committed look journal), sibling of `lastDieFace`. Deck occupancy unchanged is not the look. Target is the looked card (`cardIn("deck", …)`), not the hero. — AZL004 — `packages/engine/src/testing/test-engine.ts` + `packages/cards/src/cards/equipment/wayfinder-s-crest.test.ts` (2026-08-30)
- [keyword] CR 8.3.25 Mirage close-destroy is any defending origin, not only equipment. Blade Break / Temper stay equipment-only. Hand-played Mirage DRs emit `destroy` at chain-close (Flicker Trick vs 6{p} GY; vs 4{p} stays on the chain through damage). — DTD218 — `packages/engine/src/procedures/combat/combat-close.ts` + `packages/cards/src/cards/defense-reactions/flicker-trick.test.ts` (2026-08-30)
- [amount] printed Instant “Destroy this: Discard a card. Gain {r} equal to its pitch” is destroy-self cost plus resolution discard then reference `it` pitch. Empty hand is a legal activation (discard is not a cost); failed discard leaves `it` empty so `missing:"zero"` gains 0{r}, not “unsupported amount”. — AGB005 — `packages/cards/src/cards/equipment/graven-justaucorpse.test.ts` (2026-08-30)
- [play] printed “you may turn it face-up. If you do, you may play it this turn as though it were an instant” is optional turn-face-up `outputBinding:it` then a duration `play-card` grant, never `optional` wrapping play-card. Defender Instant needs attacker `pass()` first. — HNT253 — `packages/cards/src/cards/defense-reactions/chain-reaction.test.ts` (2026-08-30)
- [play] printed “If a card with 6 or more {p} is banished this way, you may play it from your banished zone during your next action phase” is a resolution `play-card` grant on `banished-this-way` with `duration: during-own-next-action-phase`, never a play-static permission on the DR itself and never `optional` wrapping play-card. Wire that duration in `continuousDurationSupported` / `resolveContinuousExpiry` (`player-action-phase-window`, own seat, turn+2 if generated on own turn else +1) and the play-card duration allowlist. Unique 6{p} other-hand card auto-binds the random cost; skip the windowed action phase to prove expiry. — DTD110 — `packages/engine/src/rules/proposals/shared.ts` + `packages/cards/src/cards/defense-reactions/expendable-limbs.test.ts` (2026-08-30)
- [amount] printed “Nth or higher chain link in a row to hit” is `compare-amount` of `consecutive-chain-links-that-hit` (trailing run of `closedLinkHits` plus current `didHit`), never absolute `chain-link-count`. “Control less equipment than all other heroes” is `compare-amount` of Equipment `cards-in-zone` vs opponent, never a has-status slug. `greatest-base-stat-among` requires `property` or throws. — WTR079 / IRA003 / DYN029 — `packages/engine/src/rules/evaluation/amounts/count.ts` (2026-09-07)
- [keyword] printed Shadow Resist N is a Ward-like optional prevention that destroys the source if the damage source’s controller has Shadow (supertype, including generic attacks). It cannot prevent loss of life. — IAR227 / IAR088 — `packages/engine/src/kernel/replacements/collect.ts` + `packages/cards/src/cards/equipment/dark-arcanite-boots.test.ts` (2026-09-11)
- [keyword] Unique GSA is by moniker (hero/demi-hero outranks tokens), on enter-arena and fixture seating. Incarnate destroy → `ceaseEphemeralCard` (no dies). Usurp additional cost scans any public arena Runechant. Extra traverse layers fail once the generating face is no longer showing (`definition.layout.back.faceId`). — IAR009 / IAR064 / IAR106 — `packages/engine/src/rules/reducers/shared.ts` + `packages/engine/src/procedures/play-card/effect-costs.ts` (2026-09-11)
- [order] beginning-of-end-phase blood-debt runs before decay so Corpses created from decay deaths miss that check. Ally heal at `reset-assets` is every ally (CR 8.2.8b / 4.4.3a), not controller-only. Prove Decay-before-heal by damaging the turn player's own Decay ally on that same end phase. — IAR053 / IAR064 — `packages/engine/src/procedures/turn/stages/turn.ts` + `packages/engine/src/procedures/turn/stages/cleanup.ts` (2026-09-11)
- [assert] evaluated color includes Purple (Soul of Existence pitch 4). — IAR000 — `packages/engine/src/testing/fluent-assert.ts` (2026-09-11)
- [harness] Viserai, the Forsaken seating is single-faced; traverse proofs use `PHYSICAL_STRUCTURED_CARDS_BY_CANONICAL_ID` physical twin. Deck is bottom-first so Soul-of-Existence effect-pitch must be last. `playAttack` `target` string is the attack target (ally instance ids). — IAR106 / IAR000 — `packages/engine/src/testing/test-fixtures.ts` (2026-09-11)
- [clash continuation] `procedures/layer-resolution/sequence.ts` commits a clash prefix before proposing dependent movement/conditions, then resumes the same layer from committed bindings. Retire rebased prefix guards and freeze already-entered conditional branches; a second clash must not reselect the first clash's branch. Trounce + Victor accepts/declines a real Gold-funded replacement, then token reward and exact drawn reveal — `packages/cards/src/cards/blocks/trounce.test.ts` (2026-09-19).

- Player sharpen history: `performed-this-turn/sharpen-sword` reads `history.turn.sharpenedSwordThisTurn`, stamped at the committed sharpen reducer for the performing player and Sword event snapshot; Edge Laden Plate proves positive, absent, next-turn, opponent and Cash Out removal boundaries (`cards/equipment/edge-laden-plate.test.ts`, 2026-09-19).

- Private banish observation (CR 3.0.7a): `rules/banish-observation.ts` masks properties only when private at origin and destination; trigger filters must use their declared observed object and banished-power history must share that policy. Hooves + Barraging and real Levia rules scenarios cover public/private contrasts (2026-09-19).

- Evo transform (CR 8.5.36): reuse `transform-into-resolving-card` grouped sources; choose optional Drivers before atomic movement, distinguish empty optional groups from missing required Base, and keep Evo face unchanged. Breaker equip/X/expiry/boost suites plus Invocation/Construct regressions; old tests asserting graveyard were invalid proof (2026-09-19).

- [trigger versus effect condition] CR 6.6.1c/d: Heads Up "When this attacks, if it wagered" uses an event trigger plus conditional resolution, preserving true event-and-state checks. All Gutshot pitches prove both controller-chosen orders through defense legality; do not globally delay trigger-state evaluation. `packages/cards/src/cards/actions/heads-up.test.ts` (2026-09-20).

- [trigger] Steal actor is effect controller (CR8.5.54a), while moved-object retains prior controller. Killjoy must not require pre-steal control; actor filter plus Gold/reason filter and any object relationship. Real Cutpurse Gold/no-Gold/two-Killjoy proof — `packages/cards/src/cards/heroes/killjoy-the-crooked-blade.test.ts` (2026-09-20).

- [continuation] Shared for-each branch suffix must retain qualifying hero cohort before prefix commit, not freeze all subjects true. Existing count/filter binding plus typed sequence continuation; Spur Locked both winner directions and tie — `packages/engine/src/procedures/layer-resolution/sequence.ts` (2026-09-20).

- [target] CR1.8.5d damage "any target" uses declared object hero/permanent zones plus hasProperty life; any-hero excludes allies and an unfiltered object target admits equipment. Strike Twice proves ally death/unchanged hero history and rejected equipment choice with continued legal hero selection; use public playInstance explicit drive to avoid legacy auto-selection — `packages/cards/src/cards/actions/strike-twice.test.ts` (2026-09-20).

- [prevention] CR 6.4.10j printed “prevent the next N” is shielding prevention: unused capacity carries across qualifying events until exhausted or expired. Do not encode it as one-off fixed prevention. Misfire Dampener, Templar Spellbane, and Runebleed Robe prove conditional amount, packet carry, damage type, duration, mixed destroy costs, and equipment keyword boundaries — `packages/cards/src/cards/equipment/misfire-dampener.test.ts` + `packages/cards/src/cards/equipment/templar-spellbane.test.ts` + `packages/cards/src/cards/equipment/runebleed-robe.test.ts` (2026-09-20).
