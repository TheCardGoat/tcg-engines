# st04 — Tests blocked on engine mechanics

## Conversion summary

Converted in this sweep (6 files, 8 tests):

- `base/015-archangel.test.ts`, `base/016-vesalius.test.ts` — Deploy adds
  shield; Burst deploySelf.
- `pilot/010-kira-yamato.test.ts`, `pilot/011-athrun-zala.test.ts` —
  Burst addSelfToHand.
- `unit/002-strike-gundam.test.ts` — Deploy draw 1, discard 1 (deck
  delta + trash delta).
- `unit/004-moebius.test.ts`, `unit/007-aegis-gundam-ma-mode.test.ts` —
  card-data keyword assertions (Blocker / Breach 3).

## Blocker buckets

### A. Command-card 【Burst】 dispatch — not yet wired

Same as st01 §A.

- `command/012-striker-pack.ts` (Burst deployToken)

### B. `<Blocker>` direct-attack gating

Same as st01 §B.

- ~~`unit/004-moebius.ts`~~ ✅ — converted in wave-5 resweep; Wave-15 adds `expectAttackRedirectedTo` assertion.
- ~~`base/016-vesalius.ts`~~ ✅ Wave-15 — activate-on-base AP+1 buff via `activateBaseAbility`.

### C. Pilot-resident `attack` / `destroyed` triggers

~~Same as st02 §C.~~ See st02 §C for resolution notes.

- ~~`pilot/010-kira-yamato.ts` (Attack AP-2 on chosen enemy)~~ — converted via `resolveCombat` + pilot-pair fixture.
- ~~`unit/009-miguel-s-ginn.ts` (duringPair + destroyed draw)~~ — **resolved**
  by trigger-routing fix (see st02 §C); card test now covers the paired,
  unpaired, and no-other-link-unit cases.

### D. `cantAttack` ignoring `count` / chosenTargets

Archangel's activated ability: "Choose 1 Blocker. Set it active. It can't
attack during this turn." — the second clause's `cantAttack` over-applies,
so card-data has `directives: []`.

- `base/015-archangel.ts`

### E. ~~`whenLinked` without card-level `linkCondition`~~ — **RESOLVED (test-side)**

~~Rule 3-2-6 gates `whenLinked` on the pilot satisfying the unit's
`linkCondition`. Cards without `linkCondition` have an ungated trigger.~~

Resolved in `fix/card-data-repair-wave5` — Athrun Zala's pilot test now
exercises the whenLinked wiring by pairing onto a mock unit whose
`linkCondition: "[Athrun Zala]"` admits a link pairing. The printed
`chooseAttackTarget` side effect remains delegated (empty directives —
target-override is a runtime attack-time rule).

- ~~`pilot/011-athrun-zala.ts` (plus `chooseAttackTarget` blocker)~~

### F. `<Breach N>` shield-area damage helper

Exercising `<Breach N>` requires a full enterBattle → damage-resolve
sequence where the attacker out-kills the defender and the trigger deals
specified damage to a top shield. No card-level test drives this.

- `unit/007-aegis-gundam-ma-mode.ts`
