# st03 — Tests blocked on engine mechanics

## Conversion summary

Converted in this sweep (6 files, 8 tests):

- `base/015-rewloola.test.ts` — Deploy adds shield + deals 1 damage to a
  ≤5 AP enemy; Burst deploySelf.
- `base/016-falmel.test.ts` — Deploy adds shield + deploys a Char's Zaku
  II token; Burst deploySelf.
- `pilot/010-full-frontal.test.ts`, `pilot/011-char-aznable.test.ts` —
  Burst addSelfToHand.
- `unit/008-zaku.test.ts` — Attack trigger AP+2 during this turn (via
  enterBattle → findStatModifier).
- `unit/009-gouf.test.ts` — Deploy deployToken (Zaku II token).

## Blocker buckets

### A. Command-card 【Burst】 dispatch — not yet wired

Same as st01 §A.

- `command/013-close-combat.ts` (Burst → Activate Main)

### C. `duringLink` / `duringPair` / `attack` triggers

Same as st02 §C — exercising these needs paired units and combat.

- ~~`pilot/011-char-aznable.ts` (Attack statModifier AP+1)~~ —
  **RESOLVED** in `fix/pilot-card-routing-scan`: pilot-resident
  multi-directive 【Attack】 trigger with `target: { owner: "self",
cardType: "unit" }` now resolves "self" to the paired unit (rule
  3-3-9-1). The `grantKeyword <High-Maneuver>` second directive still
  needs combat-flow plumbing — see §F below.

### D. `preventDamage` ignoring `count` / chosenTargets

`preventDamage` in the executor uses `evaluateTargetFilter` directly,
ignoring `count` and chosenTargets — a "Choose 1" filter over-applies.
Card-data currently has `directives: []` to avoid the over-fire.

- `command/014-the-blue-giant.ts`

### E. ~~Trait OR-filter in target selection~~ — **RESOLVED**

~~`attributeFilters` are AND-only. Full Frontal's "Choose 1 (Neo Zeon)/(Zeon)
Unit" needs an OR primitive.~~

Resolved by PR #113's `AttributeFilter.or` primitive + PR #118 data-sweep
restoration. Directives now use the `or`-shaped `attributeFilters`.

- ~~`pilot/010-full-frontal.ts`~~ ✅

### F. `<High-Maneuver>` keyword — unimplemented

Char Aznable's Attack trigger grants `<High-Maneuver>` (can't be blocked)
which isn't honored by the engine's combat flow.

- `pilot/011-char-aznable.ts`

### G. ~~`<Support N>` keyword + empty directives~~ — **RESOLVED**

~~st03 cards printed with `<Support N>` have `keywordEffects: []` and
`directives: []` on the activated ability. Support is synthesized from
`keywordEffects`, so `useSupport` returns `NO_SUPPORT_KEYWORD`.~~

Resolved in `fix/card-data-repair-wave5` — `keywordEffects: [{ keyword:
"Support", value: N }]` restored; activated-directive remains empty
(synthesized from the keyword).

- ~~`unit/002-angelo-s-geara-zulu.ts`~~
- ~~`unit/004-gaza-d-sleeves.ts`~~
