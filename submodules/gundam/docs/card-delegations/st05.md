# st05 — Tests blocked on engine mechanics

## Conversion summary

Converted in this sweep (5 files, 7 tests):

- `base/015-isaribi.test.ts` — Deploy adds shield; Burst deploySelf.
- `pilot/010-mikazuki-augus.test.ts` — Burst addSelfToHand;
  whenPaired deals 1 damage to chosen enemy.
- `pilot/011-akihiro-altland.test.ts`, `pilot/012-mcgillis-fareed.test.ts`
  — Burst addSelfToHand.
- `unit/003-cgs-mobile-worker.test.ts` — Activate Main with restSelf cost:
  chosen friendly takes 1 damage + gains AP+1.
- `unit/008-graze-commander-type.test.ts` — card-data Blocker assertion.

## Blocker buckets

### A. Command-card 【Burst】 dispatch — not yet wired

Same as st01 §A.

- `command/014-fatal-strike.ts` (Burst dealDamage)
- `command/014-unforeseen-incident.ts` (Burst → Activate Main)

### B. `<Blocker>` direct-attack gating

Same as st01 §B.

- ~~`unit/008-graze-commander-type.ts`~~ ✅ — converted in wave-5 resweep; Wave-15 adds `expectAttackRedirectedTo` assertion.

### C. `duringLink` / `destroyed` combat-reactive triggers

~~Same as st02 §C.~~ Trigger-routing gap fixed (see st02 §C). Remaining
issues below are per-card data / directive gaps.

- `pilot/011-akihiro-altland.ts` (duringLink + destroy + addFromTrash —
  card data also has `directives: []` because the `addFromTrash` count
  semantics are unreliable) — **timing fixed (Wave 7)**. ~~Combat
  observer-scan gap (`handleUnitDefeated`) blocks the engine path~~ —
  **observer scan landed (PR #124)**. ~~Still blocked on the
  pilot-resident passive-scan gap~~ — **paired pilots are in battleArea
  and observer-scanned (PR #122)**; ~~resolveCombat zone-commit
  ordering bug blocks harness assertions~~ — **resolved in
  `fix/resolve-combat-zone-commit`** (`markAsLinkUnit` synthetic pilot
  defaults `apBonus`/`hpBonus: 0`, so paired-unit combat math is
  well-formed). The end-to-end "add a Tekkadan unit from trash to
  hand" assertion still waits on `addFromTrash` directive wiring.
- ~~`unit/005-gundam-gusion-rebake.ts` (Destroyed rest enemy)~~ — converted via `resolveCombat` destroy fixture.

### ~~D. Activated-ability on a base with a damaged-unit filter~~ RESOLVED (Wave 15)

Unblocked by `TestPlayerState.baseSection` + `activateBaseAbility`; chosen
damaged friendly gets AP+2 with the `state: "damaged"` target filter.

- ~~`base/015-isaribi.ts`~~ ✅

### E. Unconditional directives masking printed preconditions

~~Card data does not encode the printed "if you have 2 or more other
(Gjallarhorn)/(Tekkadan) Units in play" precondition; the directive runs
unconditionally. Fixing the data also requires a trait OR-primitive.~~

~~- `pilot/012-mcgillis-fareed.ts`~~

**RESOLVED** in `feat/effect-condition-multi-trait` —
`EffectCondition.unitCount.hasTrait` now accepts `string | string[]` (array
= OR), and the precondition is encoded on McGillis Fareed. Test converted
to a real end-to-end scenario.

### G. Conditional constant effects (`selfIsDamaged`, etc.)

Constant effects gated on `selfIsDamaged` are evaluated inside
`getEffectiveStats`. No card-level test exercises the conditional-constant
path today.

- `unit/002-gundam-barbatos-2nd-form.ts`
