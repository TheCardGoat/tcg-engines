# st06 — Tests blocked on engine mechanics

## Conversion summary

Converted in this sweep (6 files, 8 tests):

- `base/014-clan-battle.test.ts`, `base/015-kaneban-co-ltd.test.ts` —
  Deploy adds shield; Burst deploySelf.
- `pilot/009-amate-yuzuriha-machu.test.ts`, `pilot/010-shuji-it.test.ts`
  — Burst addSelfToHand.
- `unit/002-gquuuuuux-omega-psycommu.test.ts` — Deploy dealDamage to
  chosen enemy (precondition separately tracked, see §H).

## Blocker buckets

### A. Command-card 【Burst】 dispatch — not yet wired

Same as st01 §A.

- `command/013-close-combat.ts` (Burst → Activate Main)

### C. `duringLink` + `attack` triggers + conditional thenDirectives

~~Same as st02 §C.~~ Trigger-routing gap fixed (see st02 §C).
Shuji also needs top-or-bottom deck routing as `thenDirectives` (currently
empty).

- `pilot/010-shuji-it.ts` — **engine-routed**; still awaits `thenDirectives`
  wiring for the top/bottom-of-deck choice.
- `base/015-kaneban-co-ltd.ts` (on-link grantKeyword <Breach 3>)

### D. `preventDamage` / activated-base buffs ignoring count

Same as gd02 §B / st03 §D — card-data has `directives: []`.

- `command/013-fierce-unity.ts`
- ~~`base/014-clan-battle.ts`~~ ✅ Wave-15 — baseline AP+2 buff via
  `activateBaseAbility` converted. Printed "(Clan) Link Unit" precondition
  still not encoded in card data (tracked separately under §H).

### E. ~~`whenLinked` without card-level `linkCondition`~~ — **RESOLVED**

~~Same as st04 §E.~~

Resolved in `fix/card-data-repair-wave5` — Amate's pilot test pairs onto
a mock unit with `linkCondition: "[Amate Yuzuriha]"` and asserts the
auto-resolving `lookAtTopDeck + tutorFilter` moves a Clan top-deck card
to hand (deck count decrements), while the non-link pairing leaves the
deck untouched.

- ~~`pilot/009-amate-yuzuriha-machu.ts` (plus `tutorFilter` single-card peek)~~

### F. `chooseAttackTarget` / tutorFilter reveal-and-pick

`chooseAttackTarget` as a structured effect isn't exercised by any
card-level test; single-card `tutorFilter` peeks with chooseTop return
mode also lack observable assertions.

- `unit/007-ortega-s-rick-dom-gq.ts`
- `command/012-schoolgirl-and-smuggler.ts` (pre-existing skip)

### G. ~~`<Support N>` keyword + empty directives~~ — **RESOLVED**

~~Same as st03 §G.~~

Resolved in `fix/card-data-repair-wave5`.

- ~~`unit/003-gaia-s-rick-dom-gq.ts`~~

### H. Unconditional directives masking printed preconditions

Same as st05 §E — card data's directive runs unconditionally, ignoring the
printed "if another friendly (Clan) Unit is in play" gate.

- `unit/002-gquuuuuux-omega-psycommu.ts`
