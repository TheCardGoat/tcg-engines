# References

Pinned external knowledge that the agent should be able to reach without web access.

Use the `*-llms.txt` convention for LLM-optimized digests of upstream docs.

## Suggested contents (add as needed)

- `gundam-tcg-rules.txt` — distilled official rules. The skill at `.claude/skills/gundam-tcg-rules.md` is the working version; promote stable content here.
- `vite-plus.txt` — Vite+ CLI surface. Today this lives in `CLAUDE.md`; copy stable bits here as a faster lookup.
- `react-router-7.txt` — RR7 surface that the mounted simulator module actually uses.
- `paraglide.txt` — i18n flow used by the migrated simulator module.

Keep each file flat: headings, short bullets, code snippets. Avoid long prose.
