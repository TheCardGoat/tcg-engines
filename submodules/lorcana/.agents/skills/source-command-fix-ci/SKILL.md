---
name: source-command-fix-ci
description: Compatibility router for the migrated fix-ci source command. Use only when an older prompt explicitly invokes source-command-fix-ci.
---

# Fix CI Compatibility Router

Read and follow the repository's canonical
[`fix-ci` command](../../../../../.agents/commands/fix-ci.md). Read the current
`AGENTS.md` chain before acting.

Do not duplicate CI commands or delegation rules here. The root command owns
the workflow.
