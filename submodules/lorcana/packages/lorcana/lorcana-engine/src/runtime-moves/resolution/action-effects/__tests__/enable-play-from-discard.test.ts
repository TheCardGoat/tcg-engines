import { describe, it } from "bun:test";

/**
 * `enable-play-from-discard` grants a temporary permission to play a specific
 * discarded card through the normal play-card move. End-to-end coverage lives
 * with the card behavior that creates the permission.
 */
describe("enable-play-from-discard", () => {
  it.todo("unit: add enable-play-from-discard coverage once the enable-registry harness exists");
});
