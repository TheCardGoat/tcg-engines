/**
 * Authored bot-lab archetype decks now live in `@tcg/cyberpunk-utils` so the
 * server-adapter practice catalog and the simulator practice fixtures share
 * the same source as this bench. Re-exported here to keep existing imports
 * stable.
 */
export { authoredBotLabDeckSpecs } from "@tcg/cyberpunk-utils";
export type { AuthoredBotLabDeckSpec } from "@tcg/cyberpunk-utils";
