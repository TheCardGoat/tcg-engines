---
description: 
globs: 
alwaysApply: true
---

# Running SQL commands

don't run any SQL commands, create the command and ask the user to run it.

# Data Migration Guidelines

Don't ever run database migrations. All migrations MUST be run by the user.

# SQL Guidelines

Write Postgres-compatible SQL code for Supabase migration files that:

- You MUST never run any migration file, the user will run the migrations.
- Don't create scripts that run SQL code, if you need to run SQL code, ask the user to run.