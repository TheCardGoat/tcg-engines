import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { svenOfficialIceDeliverer } from "./055-sven-official-ice-deliverer";

describe("Sven - Official Ice Deliverer", () => {
  // Add ability tests here
  // Examples:
  // It("has [Keyword]", () => {
  //   Const testEngine = new LorcanaTestEngine({ play: [svenOficialIceDeliverer] });
  //   Expect(testEngine.getCardModel(svenOficialIceDeliverer).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
