import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { mickeyMousesteamboatPilot } from "./089-mickey-mouse-steamboat-pilot";

describe("Mickey Mouse - Steamboat Pilot", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [mickeyMousesteamboatPilot] });
  //   expect(testEngine.getCardModel(mickeyMousesteamboatPilot).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
