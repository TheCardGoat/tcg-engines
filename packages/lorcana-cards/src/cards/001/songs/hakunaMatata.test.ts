import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { HakunaMatataUndefined } from "./hakunaMatata";

describe("Hakuna Matata - undefined", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [HakunaMatata] });
  //   expect(testEngine.getCardModel(HakunaMatata).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
