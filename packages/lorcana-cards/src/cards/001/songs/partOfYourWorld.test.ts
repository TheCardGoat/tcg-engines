import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { PartOfYourWorldUndefined } from "./partOfYourWorld";

describe("Part of Your World - undefined", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [PartofYourWorld] });
  //   expect(testEngine.getCardModel(PartofYourWorld).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
