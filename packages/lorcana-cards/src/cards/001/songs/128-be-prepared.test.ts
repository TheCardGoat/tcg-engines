import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { BePreparedUndefined } from "./128-be-prepared";

describe("Be Prepared - undefined", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [bePrepared] });
  //   expect(testEngine.getCardModel(bePrepared).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
