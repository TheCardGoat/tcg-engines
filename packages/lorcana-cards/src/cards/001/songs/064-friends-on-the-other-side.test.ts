import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { FriendsOnTheOtherSideUndefined } from "./064-friends-on-the-other-side";

describe("Friends On The Other Side - undefined", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [friendsOnTheOtherSide] });
  //   expect(testEngine.getCardModel(friendsOnTheOtherSide).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
