import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { GrabYourSwordUndefined } from "./198-grab-your-sword";

describe("Grab Your Sword - undefined", () => {
  // Add ability tests here
  // Examples:
  // it("has [Keyword]", () => {
  //   const testEngine = new LorcanaTestEngine({ play: [grabYourSword] });
  //   expect(testEngine.getCardModel(grabYourSword).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
