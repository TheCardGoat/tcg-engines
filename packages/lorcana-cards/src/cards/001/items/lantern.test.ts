import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { lanternundefined } from "./lantern";

describe("Lantern - undefined", () => {
  // Add ability tests here
  // Examples:
  // It("has [Keyword]", () => {
  //   Const testEngine = new LorcanaTestEngine({ play: [Lantern] });
  //   Expect(testEngine.getCardModel(Lantern).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
