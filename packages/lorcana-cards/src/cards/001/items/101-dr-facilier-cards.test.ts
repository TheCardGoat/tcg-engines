import { describe, expect, it } from "bun:test";
import { LorcanaTestEngine, PLAYER_ONE } from "@tcg/lorcana/testing";
import { drFacilierundefined } from "./101-dr-facilier-cards";

describe("Dr. Facilier - undefined", () => {
  // Add ability tests here
  // Examples:
  // It("has [Keyword]", () => {
  //   Const testEngine = new LorcanaTestEngine({ play: [drFacilierCards] });
  //   Expect(testEngine.getCardModel(drFacilierCards).hasKeyword()).toBe(true);
  // });
  // TODO: Add tests for abilities
});
