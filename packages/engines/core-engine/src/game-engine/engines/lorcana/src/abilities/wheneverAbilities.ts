// STUB: Legacy "whenever" abilities module for test compatibility during migration
// This module provides type-safe stubs for the old wheneverAbilities system
import type { LorcanaTriggeredAbility } from "./triggered/triggered-ability";

// Stub type exports that tests may reference
export type WheneverAbility = LorcanaTriggeredAbility;

// Stub helper functions that card definitions may use
export function createWheneverAbility(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

// Legacy ability constructors
export function wheneverQuests(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverPlays(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverTargetPlays(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOneOfYourCharactersIsBanishedInAChallenge(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverBanishesAnotherCharacterInChallenge(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouReadyThisCharacter(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverAnotherCharIsBanished(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouPlayAFloodBorn(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouHeal(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverChallengesAnotherChar(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOneOfYourCharChallengesAnotherChar(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverIsReturnedToHand(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOppCharIsDamaged(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOpposingCharIsBanished(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYourOpponentDiscardsOneOrMore(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function duringYourTurnWheneverBanishesCharacterInChallenge(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverThisCharIsDamaged(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouPlayALocation(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOpposingCharIsBanishedInChallenge(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverACharacterQuests(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOneOfYourCharsQuests(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverThisCharSings(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouDiscardACard(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouDrawACard(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouPlayASong(config: any): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOpponentDrawsACard(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverOpponentPlaysASong(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverYouPlayACharacter(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverACharacterQuestsWhileHere(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}

export function wheneverThisCharacterDealsDamageInChallenge(
  config: any,
): LorcanaTriggeredAbility {
  return config as LorcanaTriggeredAbility;
}
