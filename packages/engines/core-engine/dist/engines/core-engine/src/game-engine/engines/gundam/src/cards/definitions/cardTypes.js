export const isGundamitoUnitCard = (card) => card?.type === "unit";
export const isGundamitoPilotCard = (card) => card?.type === "pilot";
export const isGundamitoBaseCard = (card) => card?.type === "base";
export const isGundamitoCommandCard = (card) => card.type === "command";
export const isGundamitoResourceCard = (card) => card?.type === "resource";
//# sourceMappingURL=cardTypes.js.map