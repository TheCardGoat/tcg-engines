import type { LorcanaGameState } from "../lorcana-engine-types";
/**
 * Creates an empty LorcanaGameState with additional properties needed for testing
 */
export declare function createEmptyLorcanaGameState(matchId?: string, gameId?: string, randomSeed?: string, firstPlayer?: string, playerIds?: string[]): LorcanaGameState;
//# sourceMappingURL=createEmptyLorcanaGameState.d.ts.map